diff --git a/apps/pipstart/next.config.ts b/apps/pipstart/next.config.ts
--- a/apps/pipstart/next.config.ts
+++ b/apps/pipstart/next.config.ts
@@ -31,6 +31,7 @@ export const securityHeaders = [
 ] as const;
 
 const nextConfig: NextConfig = {
+  allowedDevOrigins: ["127.0.0.1"],
   poweredByHeader: false,
   async headers() {
     return [
diff --git a/apps/pipstart/package.json b/apps/pipstart/package.json
--- a/apps/pipstart/package.json
+++ b/apps/pipstart/package.json
@@ -7,7 +7,7 @@
     "build": "next build",
     "start": "next start --port 3000",
     "test:e2e": "playwright test",
-    "test": "vitest run",
+    "test": "vitest run --exclude=e2e/**",
     "lint": "eslint .",
     "typecheck": "tsc --noEmit"
   },
diff --git a/apps/pipstart/src/lib/security-headers.test.ts b/apps/pipstart/src/lib/security-headers.test.ts
--- a/apps/pipstart/src/lib/security-headers.test.ts
+++ b/apps/pipstart/src/lib/security-headers.test.ts
@@ -7,6 +7,10 @@ describe("PipStart security headers", () => {
     expect(nextConfig.poweredByHeader).toBe(false);
   });
 
+  it("allows the isolated local browser-test origin in development", () => {
+    expect(nextConfig.allowedDevOrigins).toEqual(["127.0.0.1"]);
+  });
+
   it("applies the hardened header set to every route", async () => {
     const configuredHeaders = await nextConfig.headers?.();
 
