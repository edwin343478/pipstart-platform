diff --git a/apps/pipstart/src/app/analysis/[slug]/page.module.css b/apps/pipstart/src/app/analysis/[slug]/page.module.css
index 002d9ba..fecac16 100644
--- a/apps/pipstart/src/app/analysis/[slug]/page.module.css
+++ b/apps/pipstart/src/app/analysis/[slug]/page.module.css
@@ -69,6 +69,11 @@
   font-size: 0.75rem;
 }
 
+.meta a {
+  color: inherit;
+  text-underline-offset: 0.15rem;
+}
+
 .disclosure {
   margin-bottom: 1.5rem;
   border: 1px solid #fde68a;
@@ -96,6 +101,31 @@
   border-top: 1px solid #d9e0e5;
 }
 
+.authorBox {
+  margin-top: 1.625rem;
+  border: 1px solid #d9e0e5;
+  border-radius: 0.75rem;
+  background: #fff;
+  padding: 1rem 1.125rem;
+}
+
+.authorBox p {
+  margin: 0;
+  color: #667085;
+  font-size: 0.75rem;
+  line-height: 1.6;
+}
+
+.authorBox h2 {
+  margin: 0.25rem 0;
+  font-size: 0.9375rem;
+}
+
+.authorBox a {
+  color: #0f766e;
+  text-decoration: none;
+}
+
 .relatedLabel {
   margin: 0 0 0.125rem;
   color: #0b1220;
diff --git a/apps/pipstart/src/app/analysis/[slug]/page.tsx b/apps/pipstart/src/app/analysis/[slug]/page.tsx
index ce6b319..bf9a189 100644
--- a/apps/pipstart/src/app/analysis/[slug]/page.tsx
+++ b/apps/pipstart/src/app/analysis/[slug]/page.tsx
@@ -2,8 +2,11 @@ import Link from "next/link";
 import { notFound } from "next/navigation";
 
 import { Breadcrumbs } from "../../../components/breadcrumbs";
+import { AuthorBox } from "../../../components/author-box";
+import { JsonLd } from "../../../components/json-ld";
 import { CompactHeader } from "../../../components/site-chrome";
-import { createDynamicMetadata } from "../../../lib/seo";
+import { createDynamicMetadata, siteUrl } from "../../../lib/seo";
+import { pipStartEditorialTeam } from "../authors";
 import { analysisPosts, formatPublishedDate } from "../posts";
 import styles from "./page.module.css";
 
@@ -16,11 +19,28 @@ export async function generateMetadata({ params }: AnalysisArticlePageProps) {
   const post = analysisPosts.find((candidate) => candidate.slug === slug);
   if (!post) return {};
 
-  return createDynamicMetadata({
+  const metadata = createDynamicMetadata({
     path: `/analysis/${post.slug}`,
     title: post.title,
     description: post.excerpt,
   });
+
+  return {
+    ...metadata,
+    authors: [
+      {
+        name: pipStartEditorialTeam.name,
+        url: pipStartEditorialTeam.href,
+      },
+    ],
+    openGraph: {
+      ...metadata.openGraph,
+      type: "article",
+      publishedTime: post.publishedAt,
+      modifiedTime: post.reviewedAt,
+      authors: [pipStartEditorialTeam.href],
+    },
+  };
 }
 
 interface AnalysisArticlePageProps {
@@ -45,7 +65,7 @@ export default async function AnalysisArticlePage({
   const related = analysisPosts
     .filter(
       (candidate) =>
-        candidate.category === post.category && candidate.slug !== post.slug,
+        candidate.cluster === post.cluster && candidate.slug !== post.slug,
     )
     .sort(
       (a, b) =>
@@ -55,6 +75,23 @@ export default async function AnalysisArticlePage({
 
   return (
     <main className={styles.page}>
+      <JsonLd
+        data={{
+          "@context": "https://schema.org",
+          "@type": "Article",
+          headline: post.title,
+          description: post.excerpt,
+          datePublished: post.publishedAt,
+          dateModified: post.reviewedAt,
+          mainEntityOfPage: `${siteUrl}/analysis/${post.slug}`,
+          author: {
+            "@type": "Organization",
+            name: pipStartEditorialTeam.name,
+            url: `${siteUrl}${pipStartEditorialTeam.href}`,
+          },
+          publisher: { "@type": "Organization", name: "PipStart" },
+        }}
+      />
       <CompactHeader className={styles.header} section="Analysis" />
 
       <article className={styles.article}>
@@ -73,6 +110,14 @@ export default async function AnalysisArticlePage({
           <time dateTime={post.publishedAt}>
             {formatPublishedDate(post.publishedAt)}
           </time>
+          {" · Reviewed "}
+          <time dateTime={post.reviewedAt}>
+            {formatPublishedDate(post.reviewedAt)}
+          </time>
+          {" · "}
+          <Link href={pipStartEditorialTeam.href}>
+            {pipStartEditorialTeam.name}
+          </Link>
         </p>
 
         <div className={styles.disclosure}>
@@ -85,6 +130,11 @@ export default async function AnalysisArticlePage({
           <p key={index}>{paragraph}</p>
         ))}
 
+        <AuthorBox
+          author={pipStartEditorialTeam}
+          className={styles.authorBox}
+        />
+
         {related.length > 0 && (
           <div className={styles.related}>
             <p className={styles.relatedLabel}>Related</p>
diff --git a/apps/pipstart/src/app/analysis/authors.ts b/apps/pipstart/src/app/analysis/authors.ts
new file mode 100644
index 0000000..9cf0171
--- /dev/null
+++ b/apps/pipstart/src/app/analysis/authors.ts
@@ -0,0 +1,7 @@
+export const pipStartEditorialTeam = {
+  name: "PipStart Editorial Team",
+  slug: "pipstart-editorial-team",
+  href: "/authors/pipstart-editorial-team",
+  description:
+    "The team responsible for maintaining PipStart's plain-language Forex and cryptocurrency educational content.",
+} as const;
diff --git a/apps/pipstart/src/app/analysis/content-governance.test.tsx b/apps/pipstart/src/app/analysis/content-governance.test.tsx
new file mode 100644
index 0000000..9e04846
--- /dev/null
+++ b/apps/pipstart/src/app/analysis/content-governance.test.tsx
@@ -0,0 +1,51 @@
+import { renderToStaticMarkup } from "react-dom/server";
+import { describe, expect, it } from "vitest";
+
+import { AuthorBox } from "../../components/author-box";
+import sitemap from "../sitemap";
+import { pipStartEditorialTeam } from "./authors";
+import { analysisPosts } from "./posts";
+
+describe("analysis authorship and content organization", () => {
+  it("provides explicit publication, review, and cluster data", () => {
+    for (const post of analysisPosts) {
+      expect(Date.parse(post.publishedAt)).not.toBeNaN();
+      expect(Date.parse(post.reviewedAt)).not.toBeNaN();
+      expect(Date.parse(post.reviewedAt)).toBeGreaterThanOrEqual(
+        Date.parse(post.publishedAt),
+      );
+      expect(["fundamental", "technical"]).toContain(post.cluster);
+    }
+  });
+
+  it("keeps at least two related articles in every content cluster", () => {
+    for (const post of analysisPosts) {
+      const related = analysisPosts.filter(
+        (candidate) =>
+          candidate.cluster === post.cluster && candidate.slug !== post.slug,
+      );
+      expect(related.length).toBeGreaterThanOrEqual(2);
+    }
+  });
+
+  it("links a transparent institutional author without invented credentials", () => {
+    const markup = renderToStaticMarkup(
+      <AuthorBox author={pipStartEditorialTeam} />,
+    );
+
+    expect(markup).toContain(pipStartEditorialTeam.name);
+    expect(markup).toContain(`href="${pipStartEditorialTeam.href}"`);
+    expect(markup).toContain('aria-label="About the author"');
+  });
+
+  it("uses review dates as article sitemap modification dates", () => {
+    const entries = sitemap();
+
+    for (const post of analysisPosts) {
+      const entry = entries.find((candidate) =>
+        candidate.url.endsWith(`/analysis/${post.slug}`),
+      );
+      expect(entry?.lastModified).toEqual(new Date(post.reviewedAt));
+    }
+  });
+});
diff --git a/apps/pipstart/src/app/analysis/posts.ts b/apps/pipstart/src/app/analysis/posts.ts
index 40357d1..e61f55b 100644
--- a/apps/pipstart/src/app/analysis/posts.ts
+++ b/apps/pipstart/src/app/analysis/posts.ts
@@ -8,6 +8,8 @@ export interface AnalysisPost {
   excerpt: string;
   body: string[];
   publishedAt: string;
+  reviewedAt: string;
+  cluster: AnalysisCategory;
 }
 
 export const analysisPageCopy = {
@@ -38,6 +40,8 @@ export const analysisPosts: AnalysisPost[] = [
       "For a beginner, the useful habit isn't predicting the next move — it's noticing which part of a statement moved the market, and asking why.",
     ],
     publishedAt: "2026-09-06T09:00:00.000Z",
+    reviewedAt: "2026-09-10T09:00:00.000Z",
+    cluster: "fundamental",
   },
   {
     slug: "reading-a-jobs-report",
@@ -52,6 +56,8 @@ export const analysisPosts: AnalysisPost[] = [
       "Waiting for the initial volatility to settle before drawing any conclusion is a habit worth building early.",
     ],
     publishedAt: "2026-09-03T09:00:00.000Z",
+    reviewedAt: "2026-09-10T09:00:00.000Z",
+    cluster: "fundamental",
   },
   {
     slug: "risk-on-risk-off-explained",
@@ -65,6 +71,8 @@ export const analysisPosts: AnalysisPost[] = [
       "Recognizing this pattern helps explain days when unrelated currencies all seem to move together for no obvious individual reason.",
     ],
     publishedAt: "2026-09-01T09:00:00.000Z",
+    reviewedAt: "2026-09-10T09:00:00.000Z",
+    cluster: "fundamental",
   },
   {
     slug: "head-and-shoulders-without-forcing-it",
@@ -78,6 +86,8 @@ export const analysisPosts: AnalysisPost[] = [
       "The common mistake isn't misunderstanding the shape; it's starting to see it in almost every chart once you know what to look for. A pattern is only meaningful with the structure and volume around it to support it.",
     ],
     publishedAt: "2026-09-07T09:00:00.000Z",
+    reviewedAt: "2026-09-10T09:00:00.000Z",
+    cluster: "technical",
   },
   {
     slug: "why-old-support-does-not-always-hold",
@@ -91,6 +101,8 @@ export const analysisPosts: AnalysisPost[] = [
       "As those orders get filled or cancelled, a level's strength genuinely changes — which is why treating any single level as guaranteed is a common early mistake.",
     ],
     publishedAt: "2026-09-05T09:00:00.000Z",
+    reviewedAt: "2026-09-10T09:00:00.000Z",
+    cluster: "technical",
   },
   {
     slug: "higher-highs-higher-lows",
@@ -104,5 +116,7 @@ export const analysisPosts: AnalysisPost[] = [
       "The first warning sign isn't a single red candle — it's a low that fails to clear the prior low, breaking the pattern that defined the trend in the first place.",
     ],
     publishedAt: "2026-09-02T09:00:00.000Z",
+    reviewedAt: "2026-09-10T09:00:00.000Z",
+    cluster: "technical",
   },
 ];
diff --git a/apps/pipstart/src/app/authors/pipstart-editorial-team/layout.tsx b/apps/pipstart/src/app/authors/pipstart-editorial-team/layout.tsx
new file mode 100644
index 0000000..d27e550
--- /dev/null
+++ b/apps/pipstart/src/app/authors/pipstart-editorial-team/layout.tsx
@@ -0,0 +1,9 @@
+import type { ReactNode } from "react";
+
+import { createPageMetadata } from "../../../lib/seo";
+
+export const metadata = createPageMetadata("/authors/pipstart-editorial-team");
+
+export default function AuthorLayout({ children }: { children: ReactNode }) {
+  return children;
+}
diff --git a/apps/pipstart/src/app/authors/pipstart-editorial-team/page.tsx b/apps/pipstart/src/app/authors/pipstart-editorial-team/page.tsx
new file mode 100644
index 0000000..1d7a13b
--- /dev/null
+++ b/apps/pipstart/src/app/authors/pipstart-editorial-team/page.tsx
@@ -0,0 +1,31 @@
+import { pipStartEditorialTeam } from "../../analysis/authors";
+import styles from "../../public-page.module.css";
+import { ReferencePageShell } from "../../../components/reference-page-shell";
+
+export default function PipStartEditorialTeamPage() {
+  return (
+    <ReferencePageShell section="Authors">
+      <article>
+        <p className={styles.eyebrow}>PipStart authors</p>
+        <h1 className={styles.title}>{pipStartEditorialTeam.name}</h1>
+        <p className={styles.lead}>{pipStartEditorialTeam.description}</p>
+        <section className={styles.section}>
+          <h2>Editorial purpose</h2>
+          <p>
+            PipStart explains market concepts clearly, emphasizes risk, and
+            helps learners build knowledge without trading signals, promises of
+            profit, or personalized financial advice.
+          </p>
+        </section>
+        <section className={styles.section}>
+          <h2>Review approach</h2>
+          <p>
+            Published and reviewed dates appear on analysis articles. Reviews
+            check clarity, internal consistency, risk language, and whether
+            time-sensitive statements still match their context.
+          </p>
+        </section>
+      </article>
+    </ReferencePageShell>
+  );
+}
diff --git a/apps/pipstart/src/app/sitemap.ts b/apps/pipstart/src/app/sitemap.ts
index 2af2a3f..bf17f70 100644
--- a/apps/pipstart/src/app/sitemap.ts
+++ b/apps/pipstart/src/app/sitemap.ts
@@ -10,7 +10,7 @@ export default function sitemap(): MetadataRoute.Sitemap {
   }));
   const articles = analysisPosts.map((post) => ({
     url: new URL(`/analysis/${post.slug}`, siteUrl).href,
-    lastModified: new Date(post.publishedAt),
+    lastModified: new Date(post.reviewedAt),
   }));
   const lessons = forexLessons
     .slice(1)
diff --git a/apps/pipstart/src/components/author-box.tsx b/apps/pipstart/src/components/author-box.tsx
new file mode 100644
index 0000000..327aa54
--- /dev/null
+++ b/apps/pipstart/src/components/author-box.tsx
@@ -0,0 +1,18 @@
+import Link from "next/link";
+
+type AuthorBoxProps = {
+  author: { description: string; href: string; name: string };
+  className?: string;
+};
+
+export function AuthorBox({ author, className }: AuthorBoxProps) {
+  return (
+    <aside className={className} aria-label="About the author">
+      <p>Written and reviewed by</p>
+      <h2>
+        <Link href={author.href}>{author.name}</Link>
+      </h2>
+      <p>{author.description}</p>
+    </aside>
+  );
+}
diff --git a/apps/pipstart/src/components/reference-page-shell.tsx b/apps/pipstart/src/components/reference-page-shell.tsx
index 1a24bdc..a090b22 100644
--- a/apps/pipstart/src/components/reference-page-shell.tsx
+++ b/apps/pipstart/src/components/reference-page-shell.tsx
@@ -5,7 +5,7 @@ import styles from "./reference-page-shell.module.css";
 
 type ReferencePageShellProps = {
   children: ReactNode;
-  section: "About" | "Contact" | "Legal" | "Start Here";
+  section: "About" | "Authors" | "Contact" | "Legal" | "Start Here";
 };
 
 export function ReferencePageShell({
diff --git a/apps/pipstart/src/lib/seo.ts b/apps/pipstart/src/lib/seo.ts
index 203ba56..21bbbc6 100644
--- a/apps/pipstart/src/lib/seo.ts
+++ b/apps/pipstart/src/lib/seo.ts
@@ -32,6 +32,12 @@ export const seoEntries = [
     description:
       "Contact information and support guidance for PipStart learners.",
   },
+  {
+    path: "/authors/pipstart-editorial-team",
+    title: "PipStart Editorial Team",
+    description:
+      "Meet the team responsible for maintaining PipStart's educational market content.",
+  },
   {
     path: "/learn/forex",
     title: "Learn Forex",
