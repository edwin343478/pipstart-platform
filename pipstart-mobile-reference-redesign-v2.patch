diff --git a/apps/pipstart/src/app/analysis/page.module.css b/apps/pipstart/src/app/analysis/page.module.css
index 8dfc3bc..4d4b55b 100644
--- a/apps/pipstart/src/app/analysis/page.module.css
+++ b/apps/pipstart/src/app/analysis/page.module.css
@@ -212,6 +212,9 @@
 }
 
 @media (max-width: 640px) {
+  .header {
+    display: none;
+  }
   .header,
   .introduction {
     padding-right: 1.25rem;
@@ -222,6 +225,35 @@
     padding-right: 1.25rem;
     padding-left: 3.5rem;
   }
+  .introduction {
+    padding: 14px 18px 10px;
+    border-bottom: 1px solid #d9e0e5;
+    background: #fff;
+  }
+  .introduction h1 {
+    font-size: 19px;
+  }
+  .card {
+    border-radius: 11px;
+    padding: 13px;
+  }
+  .card h2 {
+    font-size: 13px;
+  }
+  .card p {
+    display: none;
+  }
+  .tabs {
+    width: 100%;
+    border-radius: 9px;
+    padding: 3px;
+  }
+  .tab,
+  .tabActive {
+    padding: 7px 0;
+    border-radius: 7px;
+    font-size: 11.5px;
+  }
 
   .feed::before {
     left: 1.6875rem;
diff --git a/apps/pipstart/src/app/brokers/page.module.css b/apps/pipstart/src/app/brokers/page.module.css
index 096c522..4a56d52 100644
--- a/apps/pipstart/src/app/brokers/page.module.css
+++ b/apps/pipstart/src/app/brokers/page.module.css
@@ -247,6 +247,10 @@
 }
 
 @media (max-width: 640px) {
+  .header,
+  .footer {
+    display: none;
+  }
   .header,
   .footer {
     padding-right: 1.25rem;
@@ -254,12 +258,39 @@
   }
 
   .content {
-    width: calc(100% - 2.5rem);
-    padding-top: 2.75rem;
+    width: 100%;
+    padding: 0 18px 18px;
+  }
+  .introduction {
+    margin: 0 -18px 10px;
+    padding: 14px 18px 10px;
+    border-bottom: 1px solid #d9e0e5;
+    background: #fff;
+  }
+  .introduction h1 {
+    font-size: 19px;
+  }
+  .disclosure {
+    border-radius: 9px;
+    padding: 9px 11px;
+    font-size: 10.5px;
+    line-height: 1.5;
+  }
+  .creative {
+    display: none;
+  }
+  .broker {
+    display: block;
+    border-radius: 12px;
   }
 
   .brokerDetails {
-    padding: 1.5rem;
+    padding: 14px;
+  }
+  .summary,
+  .tagline {
+    font-size: 11px;
+    line-height: 1.5;
   }
 
   .facts {
diff --git a/apps/pipstart/src/app/globals.css b/apps/pipstart/src/app/globals.css
index 4d84f72..512e790 100644
--- a/apps/pipstart/src/app/globals.css
+++ b/apps/pipstart/src/app/globals.css
@@ -57,6 +57,30 @@ body {
   font-family: var(--font-pipstart-body), Arial, sans-serif;
 }
 
+.sr-only {
+  position: absolute;
+  width: 1px;
+  height: 1px;
+  overflow: hidden;
+  clip: rect(0, 0, 0, 0);
+  clip-path: inset(50%);
+  white-space: nowrap;
+}
+
+@media (max-width: 640px) {
+  body {
+    padding-bottom: calc(3.75rem + env(safe-area-inset-bottom));
+  }
+
+  button,
+  a,
+  input,
+  select,
+  summary {
+    -webkit-tap-highlight-color: transparent;
+  }
+}
+
 h1,
 h2,
 h3,
diff --git a/apps/pipstart/src/app/glossary/page.module.css b/apps/pipstart/src/app/glossary/page.module.css
index b257be0..3c16089 100644
--- a/apps/pipstart/src/app/glossary/page.module.css
+++ b/apps/pipstart/src/app/glossary/page.module.css
@@ -193,6 +193,10 @@
 }
 
 @media (max-width: 640px) {
+  .header,
+  .footer {
+    display: none;
+  }
   .header,
   .footer {
     padding-right: 1.25rem;
@@ -200,7 +204,13 @@
   }
 
   .introduction {
-    padding-top: 2.75rem;
+    width: 100%;
+    padding: 14px 18px 12px;
+    border-bottom: 1px solid #d9e0e5;
+    background: #fff;
+  }
+  .introduction h1 {
+    font-size: 19px;
   }
 
   .results {
@@ -208,7 +218,16 @@
   }
 
   .term {
-    padding: 1.1rem 1.25rem;
+    margin-bottom: 0.5rem;
+    border-radius: 10px;
+    padding: 12px 14px;
+  }
+  .term h3 {
+    font-size: 13px;
+  }
+  .term p {
+    font-size: 11.5px;
+    line-height: 1.5;
   }
 }
 
diff --git a/apps/pipstart/src/app/layout.tsx b/apps/pipstart/src/app/layout.tsx
index fff6bbb..d27c7fc 100644
--- a/apps/pipstart/src/app/layout.tsx
+++ b/apps/pipstart/src/app/layout.tsx
@@ -5,6 +5,7 @@ import { Inter, Manrope } from "next/font/google";
 import { pipStartBrand } from "@repo/brand";
 
 import { JsonLd } from "../components/json-ld";
+import { BottomTabBar } from "../components/bottom-tab-bar";
 import { siteUrl } from "../lib/seo";
 
 import "./globals.css";
@@ -65,6 +66,7 @@ export default function RootLayout({
           }}
         />
         {children}
+        <BottomTabBar />
       </body>
     </html>
   );
diff --git a/apps/pipstart/src/app/learn/crypto/level-1/page.module.css b/apps/pipstart/src/app/learn/crypto/level-1/page.module.css
index 93cd12e..ead5e56 100644
--- a/apps/pipstart/src/app/learn/crypto/level-1/page.module.css
+++ b/apps/pipstart/src/app/learn/crypto/level-1/page.module.css
@@ -220,6 +220,13 @@
   color: #0f766e;
 }
 
+.sidebarSummary {
+  display: none;
+}
+.sidebarDetails .sidebar {
+  display: block;
+}
+
 @media (max-width: 760px) {
   .header {
     align-items: flex-start;
@@ -236,10 +243,40 @@
     grid-template-columns: 1fr;
   }
 
+  .sidebarSummary {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    border-bottom: 1px solid #d9e0e5;
+    background: #fff;
+    padding: 0.875rem 1.25rem;
+    font-size: 0.875rem;
+    font-weight: 700;
+    list-style: none;
+    cursor: pointer;
+  }
+  .sidebarSummary::-webkit-details-marker {
+    display: none;
+  }
+  .sidebarSummary svg {
+    width: 0.875rem;
+    fill: none;
+    stroke: #667085;
+    stroke-width: 2;
+  }
+  .sidebarDetails[open] .sidebarSummary svg {
+    transform: rotate(180deg);
+  }
+  .sidebarDetails:not([open]) .sidebar {
+    display: none;
+  }
   .sidebar {
     border-right: 0;
     border-bottom: 1px solid #d9e0e5;
-    padding: 1.5rem 1.25rem;
+    padding: 1rem 1.25rem 1.25rem;
+  }
+  .sidebar h2 {
+    display: none;
   }
 
   .lesson {
diff --git a/apps/pipstart/src/app/learn/crypto/level-1/page.tsx b/apps/pipstart/src/app/learn/crypto/level-1/page.tsx
index 71b7266..1cfcd02 100644
--- a/apps/pipstart/src/app/learn/crypto/level-1/page.tsx
+++ b/apps/pipstart/src/app/learn/crypto/level-1/page.tsx
@@ -28,29 +28,37 @@ export default function CryptoLevelOnePage() {
       />
 
       <div className={styles.lessonLayout}>
-        <aside className={styles.sidebar} aria-label="Bitcoin lessons">
-          <h2>Bitcoin</h2>
-          <nav>
-            {lessons.map((lesson) => {
-              const className =
-                lesson.status === "current"
-                  ? styles.currentLesson
-                  : styles.upcomingLesson;
+        <details className={styles.sidebarDetails}>
+          <summary className={styles.sidebarSummary}>
+            <span>Bitcoin</span>
+            <svg aria-hidden="true" viewBox="0 0 20 20">
+              <path d="m5 8 5 5 5-5" />
+            </svg>
+          </summary>
+          <aside className={styles.sidebar} aria-label="Bitcoin lessons">
+            <h2>Bitcoin</h2>
+            <nav>
+              {lessons.map((lesson) => {
+                const className =
+                  lesson.status === "current"
+                    ? styles.currentLesson
+                    : styles.upcomingLesson;
 
-              return (
-                <span
-                  aria-current={
-                    lesson.status === "current" ? "page" : undefined
-                  }
-                  className={className}
-                  key={lesson.label}
-                >
-                  {lesson.label}
-                </span>
-              );
-            })}
-          </nav>
-        </aside>
+                return (
+                  <span
+                    aria-current={
+                      lesson.status === "current" ? "page" : undefined
+                    }
+                    className={className}
+                    key={lesson.label}
+                  >
+                    {lesson.label}
+                  </span>
+                );
+              })}
+            </nav>
+          </aside>
+        </details>
 
         <article className={styles.lesson}>
           <p className={styles.eyebrow}>Level 1 · Lesson 1 of 6</p>
diff --git a/apps/pipstart/src/app/learn/crypto/page.module.css b/apps/pipstart/src/app/learn/crypto/page.module.css
index 191a966..398ccd6 100644
--- a/apps/pipstart/src/app/learn/crypto/page.module.css
+++ b/apps/pipstart/src/app/learn/crypto/page.module.css
@@ -204,7 +204,7 @@ article .levelMeta strong {
   }
 
   .introduction {
-    padding-top: 2.75rem;
+    padding-top: 2rem;
   }
 
   .introduction h1 {
@@ -231,7 +231,14 @@ article .levelMeta strong {
   }
 
   .levelCard {
-    padding: 1.25rem;
+    padding: 1rem;
+  }
+  .levelCard h3 {
+    font-size: 1rem;
+  }
+  .levelCard p {
+    font-size: 0.8125rem;
+    line-height: 1.5;
   }
 }
 
diff --git a/apps/pipstart/src/app/learn/forex/level-1/forex-lesson.tsx b/apps/pipstart/src/app/learn/forex/level-1/forex-lesson.tsx
index 9130dad..c9fe8d5 100644
--- a/apps/pipstart/src/app/learn/forex/level-1/forex-lesson.tsx
+++ b/apps/pipstart/src/app/learn/forex/level-1/forex-lesson.tsx
@@ -91,38 +91,49 @@ export default function ForexLessonPage({ lesson }: { lesson: ForexLesson }) {
       />
 
       <div className={styles.lessonLayout}>
-        <aside
-          className={styles.sidebar}
-          aria-label="Forex Kindergarten lessons"
-        >
-          <h2>Forex Kindergarten</h2>
-          <p className={styles.progressSummary} aria-live="polite">
-            {completedLessonSlugs.length} of {forexLessons.length} complete
-          </p>
-          <nav>
-            {forexLessons.map((candidate) => {
-              const current = candidate.slug === lesson.slug;
-
-              return (
-                <Link
-                  aria-current={current ? "page" : undefined}
-                  className={
-                    current ? styles.currentLesson : styles.upcomingLesson
-                  }
-                  href={candidate.href}
-                  key={candidate.slug}
-                >
-                  <span>{candidate.title}</span>
-                  {completedLessons.has(candidate.slug) ? (
-                    <span className={styles.completedMarker} aria-hidden="true">
-                      ✓
-                    </span>
-                  ) : null}
-                </Link>
-              );
-            })}
-          </nav>
-        </aside>
+        <details className={styles.sidebarDetails}>
+          <summary className={styles.sidebarSummary}>
+            <span>Forex Kindergarten</span>
+            <svg aria-hidden="true" viewBox="0 0 20 20">
+              <path d="m5 8 5 5 5-5" />
+            </svg>
+          </summary>
+          <aside
+            className={styles.sidebar}
+            aria-label="Forex Kindergarten lessons"
+          >
+            <h2>Forex Kindergarten</h2>
+            <p className={styles.progressSummary} aria-live="polite">
+              {completedLessonSlugs.length} of {forexLessons.length} complete
+            </p>
+            <nav>
+              {forexLessons.map((candidate) => {
+                const current = candidate.slug === lesson.slug;
+
+                return (
+                  <Link
+                    aria-current={current ? "page" : undefined}
+                    className={
+                      current ? styles.currentLesson : styles.upcomingLesson
+                    }
+                    href={candidate.href}
+                    key={candidate.slug}
+                  >
+                    <span>{candidate.title}</span>
+                    {completedLessons.has(candidate.slug) ? (
+                      <span
+                        className={styles.completedMarker}
+                        aria-hidden="true"
+                      >
+                        ✓
+                      </span>
+                    ) : null}
+                  </Link>
+                );
+              })}
+            </nav>
+          </aside>
+        </details>
 
         <article className={styles.lesson}>
           <p className={styles.eyebrow}>
@@ -139,31 +150,35 @@ export default function ForexLessonPage({ lesson }: { lesson: ForexLesson }) {
             ))}
           </section>
 
-          <div className={styles.completeAction}>
-            <button
-              aria-pressed={lessonIsComplete}
-              className={lessonIsComplete ? styles.completedButton : undefined}
-              type="button"
-              onClick={toggleCompletion}
-            >
-              <CheckIcon />
-              {lessonIsComplete ? "Completed" : "Mark complete"}
-            </button>
+          <div className={styles.stickyActions}>
+            <div className={styles.completeAction}>
+              <button
+                aria-pressed={lessonIsComplete}
+                className={
+                  lessonIsComplete ? styles.completedButton : undefined
+                }
+                type="button"
+                onClick={toggleCompletion}
+              >
+                <CheckIcon />
+                {lessonIsComplete ? "Completed" : "Mark complete"}
+              </button>
+            </div>
+
+            <LessonNavigation
+              className={styles.lessonNavigation}
+              previous={
+                previousLesson
+                  ? { href: previousLesson.href, label: previousLesson.title }
+                  : undefined
+              }
+              next={
+                nextLesson
+                  ? { href: nextLesson.href, label: nextLesson.title }
+                  : { href: "/learn/forex", label: "Return to Forex path" }
+              }
+            />
           </div>
-
-          <LessonNavigation
-            className={styles.lessonNavigation}
-            previous={
-              previousLesson
-                ? { href: previousLesson.href, label: previousLesson.title }
-                : undefined
-            }
-            next={
-              nextLesson
-                ? { href: nextLesson.href, label: nextLesson.title }
-                : { href: "/learn/forex", label: "Return to Forex path" }
-            }
-          />
         </article>
       </div>
     </main>
diff --git a/apps/pipstart/src/app/learn/forex/level-1/page.module.css b/apps/pipstart/src/app/learn/forex/level-1/page.module.css
index d84cc46..f7796d6 100644
--- a/apps/pipstart/src/app/learn/forex/level-1/page.module.css
+++ b/apps/pipstart/src/app/learn/forex/level-1/page.module.css
@@ -245,6 +245,13 @@
   color: #0f766e;
 }
 
+.sidebarSummary {
+  display: none;
+}
+.sidebarDetails .sidebar {
+  display: block;
+}
+
 @media (max-width: 760px) {
   .header {
     align-items: flex-start;
@@ -261,10 +268,40 @@
     grid-template-columns: 1fr;
   }
 
+  .sidebarSummary {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    border-bottom: 1px solid #d9e0e5;
+    background: #fff;
+    padding: 0.875rem 1.25rem;
+    font-size: 0.875rem;
+    font-weight: 700;
+    list-style: none;
+    cursor: pointer;
+  }
+  .sidebarSummary::-webkit-details-marker {
+    display: none;
+  }
+  .sidebarSummary svg {
+    width: 0.875rem;
+    fill: none;
+    stroke: #667085;
+    stroke-width: 2;
+  }
+  .sidebarDetails[open] .sidebarSummary svg {
+    transform: rotate(180deg);
+  }
+  .sidebarDetails:not([open]) .sidebar {
+    display: none;
+  }
   .sidebar {
     border-right: 0;
     border-bottom: 1px solid #d9e0e5;
-    padding: 1.5rem 1.25rem;
+    padding: 1rem 1.25rem 1.25rem;
+  }
+  .sidebar h2 {
+    display: none;
   }
 
   .lesson {
@@ -274,6 +311,21 @@
   .lesson h1 {
     font-size: 2rem;
   }
+  .stickyActions {
+    position: sticky;
+    bottom: 0;
+    margin: 1.5rem -1.25rem -2.5rem;
+    border-top: 1px solid #d9e0e5;
+    background: #fff;
+    padding: 0.75rem 1.25rem max(1rem, env(safe-area-inset-bottom));
+  }
+  .completeAction {
+    margin-bottom: 0.75rem;
+  }
+  .completeAction button {
+    width: 100%;
+    justify-content: center;
+  }
 }
 
 @media (max-width: 420px) {
diff --git a/apps/pipstart/src/app/learn/forex/page.module.css b/apps/pipstart/src/app/learn/forex/page.module.css
index 5978dcd..dcbe775 100644
--- a/apps/pipstart/src/app/learn/forex/page.module.css
+++ b/apps/pipstart/src/app/learn/forex/page.module.css
@@ -204,7 +204,7 @@ article .levelMeta strong {
   }
 
   .introduction {
-    padding-top: 2.75rem;
+    padding-top: 2rem;
   }
 
   .introduction h1 {
@@ -231,7 +231,14 @@ article .levelMeta strong {
   }
 
   .levelCard {
-    padding: 1.25rem;
+    padding: 1rem;
+  }
+  .levelCard h3 {
+    font-size: 1rem;
+  }
+  .levelCard p {
+    font-size: 0.8125rem;
+    line-height: 1.5;
   }
 }
 
diff --git a/apps/pipstart/src/app/page.module.css b/apps/pipstart/src/app/page.module.css
index d5d0293..ae7eb8e 100644
--- a/apps/pipstart/src/app/page.module.css
+++ b/apps/pipstart/src/app/page.module.css
@@ -300,25 +300,56 @@
     display: none;
   }
   .hero {
-    padding-top: 3rem;
-    padding-bottom: 2.25rem;
+    padding-top: 2.5rem;
+    padding-bottom: 1.75rem;
   }
   .hero h1 {
-    font-size: 2.5rem;
+    margin-top: 0.65rem;
+    font-size: 2.15rem;
+    line-height: 1.12;
+  }
+  .heroDescription {
+    font-size: 0.9375rem;
+    line-height: 1.55;
+  }
+  .heroTrust {
+    align-items: flex-start;
+    flex-direction: column;
+    gap: 0.45rem;
   }
   .schools,
   .resources {
     grid-template-columns: 1fr;
   }
+  .schools {
+    gap: 0.75rem;
+    padding-bottom: 1.5rem;
+  }
   .schoolCard {
-    padding: 1.5rem;
+    padding: 1.25rem;
+  }
+  .schoolCard h2 {
+    margin-block: 0.45rem;
+    font-size: 1.125rem;
+  }
+  .schoolCard div {
+    font-size: 0.875rem;
+    line-height: 1.5;
   }
   .resources a {
-    min-height: 5.5rem;
+    display: flex;
+    min-height: auto;
+    justify-content: flex-start;
+    padding: 0.9rem 1rem;
+  }
+  .resources {
+    gap: 0.5rem;
+    padding-bottom: 1.5rem;
   }
   .footer {
     align-items: flex-start;
     flex-direction: column;
+    padding-bottom: 1.25rem;
   }
   .footer nav {
     justify-content: flex-start;
@@ -329,3 +360,97 @@
     animation: none;
   }
 }
+
+@media (max-width: 640px) {
+  .header,
+  .hero,
+  .schools,
+  .footer {
+    padding-right: 18px;
+    padding-left: 18px;
+  }
+  .header {
+    min-height: 49px;
+    padding-top: 14px;
+    padding-bottom: 14px;
+  }
+  .brand {
+    font-size: 16px;
+  }
+  .menuButton {
+    min-width: 20px;
+    min-height: 20px;
+    border: 0;
+    padding: 0;
+  }
+  .menuButton svg {
+    width: 20px;
+  }
+  .hero {
+    padding-top: 26px;
+    padding-bottom: 20px;
+  }
+  .hero p {
+    font-size: 10.5px;
+    letter-spacing: 0.05em;
+  }
+  .hero h1 {
+    max-width: none;
+    margin: 10px 0 8px;
+    font-size: 24px;
+    line-height: 1.25;
+  }
+  .heroDescription {
+    font-size: 13px;
+    line-height: 1.6;
+  }
+  .heroTrust {
+    display: none;
+  }
+  .schools {
+    gap: 12px;
+    padding-top: 0;
+    padding-bottom: 18px;
+  }
+  .glowWrap {
+    border-radius: 16px;
+  }
+  .schoolCard {
+    min-height: 106px;
+    border-radius: 15px;
+    padding: 18px;
+  }
+  .schoolCard p {
+    font-size: 10px;
+  }
+  .schoolCard h2 {
+    margin: 6px 0 4px;
+    font-size: 16px;
+  }
+  .schoolCard div {
+    font-size: 11.5px;
+    line-height: 1.4;
+  }
+  .schoolCard a {
+    margin-top: 9px;
+    font-size: 12px;
+  }
+  .resources {
+    display: flex;
+    gap: 10px;
+    overflow-x: auto;
+    padding: 2px 18px 16px;
+    scrollbar-width: none;
+  }
+  .resources a {
+    flex-shrink: 0;
+    border-radius: 20px;
+    padding: 8px 14px;
+  }
+  .resources svg {
+    width: 16px;
+  }
+  .resources span {
+    font-size: 12px;
+  }
+}
diff --git a/apps/pipstart/src/app/public-page.module.css b/apps/pipstart/src/app/public-page.module.css
index 9cc3c9d..6d4418c 100644
--- a/apps/pipstart/src/app/public-page.module.css
+++ b/apps/pipstart/src/app/public-page.module.css
@@ -112,6 +112,53 @@
   color: #92400e;
 }
 
+@media (max-width: 640px) {
+  .eyebrow {
+    margin-bottom: 8px;
+    font-size: 10.5px;
+  }
+  .title {
+    margin-bottom: 12px;
+    font-size: 20px;
+    line-height: 1.3;
+  }
+  .legalTitle {
+    margin-bottom: 4px;
+  }
+  .lead {
+    margin-bottom: 20px;
+    font-size: 12.5px;
+    line-height: 1.65;
+  }
+  .updated {
+    margin-bottom: 18px;
+    font-size: 10.5px;
+  }
+  .section,
+  .legalSection {
+    margin-bottom: 16px;
+  }
+  .section h2,
+  .legalSection h2,
+  .card h2 {
+    margin-bottom: 6px;
+    font-size: 13.5px;
+  }
+  .section p,
+  .legalSection p,
+  .card p,
+  .checkList {
+    font-size: 12px;
+    line-height: 1.65;
+  }
+  .card,
+  .warning,
+  .riskNotice {
+    border-radius: 0.75rem;
+    padding: 14px;
+  }
+}
+
 .warning h2 {
   font-size: 1rem;
 }
diff --git a/apps/pipstart/src/app/tools/page.module.css b/apps/pipstart/src/app/tools/page.module.css
index 29039f9..375e0ff 100644
--- a/apps/pipstart/src/app/tools/page.module.css
+++ b/apps/pipstart/src/app/tools/page.module.css
@@ -128,7 +128,16 @@ a.tool:focus-visible {
   font-size: 0.75rem;
 }
 
+.mobileSearch,
+.mobileFilters {
+  display: none;
+}
+
 @media (max-width: 640px) {
+  .header,
+  .footer {
+    display: none;
+  }
   .header,
   .footer {
     padding-right: 1.25rem;
@@ -141,11 +150,98 @@ a.tool:focus-visible {
   }
 
   .introduction {
-    padding-top: 2.75rem;
+    width: 100%;
+    padding: 14px 18px 12px;
+    border-bottom: 1px solid #d9e0e5;
+    background: #fff;
+  }
+
+  .introduction h1 {
+    margin-bottom: 10px;
+    font-size: 19px;
+  }
+  .introduction > p {
+    display: none;
+  }
+  .mobileSearch {
+    display: flex;
+    align-items: center;
+    gap: 8px;
+    border: 1px solid #d9e0e5;
+    border-radius: 10px;
+    background: #f5f7f8;
+    padding: 9px 12px;
+  }
+  .mobileSearch svg {
+    width: 14px;
+    fill: none;
+    stroke: #8a93a0;
+    stroke-width: 1.6;
+  }
+  .mobileSearch input {
+    width: 100%;
+    border: 0;
+    outline: 0;
+    background: transparent;
+    font: inherit;
+    font-size: 12px;
+  }
+  .mobileFilters {
+    display: flex;
+    gap: 8px;
+    overflow-x: auto;
+    padding: 12px 0 8px 18px;
+    scrollbar-width: none;
+  }
+  .mobileFilters button {
+    flex-shrink: 0;
+    border: 1px solid #d9e0e5;
+    border-radius: 20px;
+    background: #fff;
+    padding: 7px 14px;
+    font: inherit;
+    font-size: 11.5px;
+    font-weight: 600;
+  }
+  .mobileFilters button[aria-pressed="true"] {
+    border-color: #0b1220;
+    background: #0b1220;
+    color: #fff;
+    font-weight: 700;
   }
 
   .tools {
+    width: 100%;
     grid-template-columns: 1fr;
+    gap: 0.5rem;
+    padding: 8px 18px 18px;
+  }
+
+  .tool {
+    display: grid;
+    grid-template-columns: 36px 1fr;
+    align-items: center;
+    gap: 0 0.75rem;
+    border-radius: 12px;
+    padding: 12px 14px;
+  }
+
+  .tool > svg {
+    grid-row: 1 / 3;
+    width: 17px;
+  }
+  .tool h2 {
+    margin: 0;
+    font-size: 13px;
+  }
+  .tool p {
+    margin-top: 0.15rem;
+    font-size: 11px;
+    line-height: 1.35;
+  }
+
+  .footer {
+    padding-bottom: 1rem;
   }
 }
 
diff --git a/apps/pipstart/src/app/tools/page.tsx b/apps/pipstart/src/app/tools/page.tsx
index 56560ee..56271a5 100644
--- a/apps/pipstart/src/app/tools/page.tsx
+++ b/apps/pipstart/src/app/tools/page.tsx
@@ -190,8 +190,25 @@ export default function ToolsPage() {
           Practical tools for the numbers every level teaches. No signup
           required.
         </p>
+        <label className={styles.mobileSearch}>
+          <span className="sr-only">Search calculators</span>
+          <svg aria-hidden="true" viewBox="0 0 20 20">
+            <circle cx="8.5" cy="8.5" r="5.5" />
+            <path d="M16 16l-3.5-3.5" />
+          </svg>
+          <input type="search" placeholder="Search calculators…" />
+        </label>
       </section>
 
+      <div className={styles.mobileFilters} aria-label="Calculator categories">
+        <button type="button" aria-pressed="true">
+          All
+        </button>
+        <button type="button">Position &amp; risk</button>
+        <button type="button">Recovery</button>
+        <button type="button">Crypto</button>
+      </div>
+
       <section className={styles.tools} aria-label="Trading calculators">
         {tools.map((tool) =>
           tool.href ? (
diff --git a/apps/pipstart/src/components/bottom-tab-bar.module.css b/apps/pipstart/src/components/bottom-tab-bar.module.css
new file mode 100644
index 0000000..38ca172
--- /dev/null
+++ b/apps/pipstart/src/components/bottom-tab-bar.module.css
@@ -0,0 +1,47 @@
+.tabBar {
+  display: none;
+}
+
+@media (max-width: 640px) {
+  .tabBar {
+    position: fixed;
+    z-index: 50;
+    right: 0;
+    bottom: 0;
+    left: 0;
+    display: flex;
+    justify-content: space-around;
+    border-top: 1px solid #d9e0e5;
+    background: rgb(255 255 255 / 96%);
+    padding: 10px 0 max(14px, env(safe-area-inset-bottom));
+    backdrop-filter: blur(10px);
+  }
+  .tab,
+  .active {
+    display: flex;
+    min-width: 3.75rem;
+    min-height: 2.5rem;
+    align-items: center;
+    flex-direction: column;
+    justify-content: center;
+    gap: 3px;
+    color: #8a93a0;
+    font-size: 9.5px;
+    font-weight: 600;
+    text-decoration: none;
+  }
+  .active {
+    color: #0f766e;
+    font-weight: 700;
+  }
+  .tab svg,
+  .active svg {
+    width: 1.25rem;
+    height: 1.25rem;
+    fill: none;
+    stroke: currentcolor;
+    stroke-linecap: round;
+    stroke-linejoin: round;
+    stroke-width: 1.6;
+  }
+}
diff --git a/apps/pipstart/src/components/bottom-tab-bar.tsx b/apps/pipstart/src/components/bottom-tab-bar.tsx
new file mode 100644
index 0000000..5e22708
--- /dev/null
+++ b/apps/pipstart/src/components/bottom-tab-bar.tsx
@@ -0,0 +1,68 @@
+"use client";
+
+import Link from "next/link";
+import { usePathname } from "next/navigation";
+
+import styles from "./bottom-tab-bar.module.css";
+
+const tabs = [
+  { href: "/", label: "Home", icon: "home" },
+  { href: "/learn/forex", label: "Learn", icon: "learn" },
+  { href: "/tools", label: "Tools", icon: "tools" },
+  { href: "/analysis", label: "Analysis", icon: "analysis" },
+] as const;
+
+const hubRoutes = new Set([
+  "/",
+  "/analysis",
+  "/brokers",
+  "/glossary",
+  "/glossary/crypto",
+  "/learn/crypto",
+  "/learn/forex",
+  "/tools",
+]);
+
+function TabIcon({ type }: { type: (typeof tabs)[number]["icon"] }) {
+  if (type === "home") return <path d="M3 9l7-6 7 6v8H12v-5H8v5H3V9Z" />;
+  if (type === "learn") return <path d="M3 4h6v12H3V4Zm8 0h6v12h-6V4Z" />;
+  if (type === "tools")
+    return (
+      <>
+        <rect x="3" y="3" width="14" height="14" rx="2" />
+        <path d="M7 8h6M7 11h4" />
+      </>
+    );
+  return <path d="m3 15 4-4 3 2 5-7 2 2" />;
+}
+
+export function BottomTabBar() {
+  const pathname = usePathname();
+  if (!hubRoutes.has(pathname)) return null;
+
+  return (
+    <nav className={styles.tabBar} aria-label="Mobile primary navigation">
+      {tabs.map((tab) => {
+        const active =
+          tab.href === "/"
+            ? pathname === "/"
+            : tab.href === "/learn/forex"
+              ? pathname.startsWith("/learn")
+              : pathname.startsWith(tab.href);
+        return (
+          <Link
+            key={tab.href}
+            href={tab.href}
+            className={active ? styles.active : styles.tab}
+            aria-current={active ? "page" : undefined}
+          >
+            <svg aria-hidden="true" viewBox="0 0 20 20">
+              <TabIcon type={tab.icon} />
+            </svg>
+            <span>{tab.label}</span>
+          </Link>
+        );
+      })}
+    </nav>
+  );
+}
diff --git a/apps/pipstart/src/components/primary-navigation.tsx b/apps/pipstart/src/components/primary-navigation.tsx
index f59d6fd..3e12276 100644
--- a/apps/pipstart/src/components/primary-navigation.tsx
+++ b/apps/pipstart/src/components/primary-navigation.tsx
@@ -44,9 +44,9 @@ export function PrimaryNavigation({
         aria-expanded={open}
         onClick={() => setOpen((current) => !current)}
       >
-        <span>Menu</span>
+        <span className="sr-only">Menu</span>
         <svg aria-hidden="true" viewBox="0 0 20 20">
-          <path d={open ? "m5 12 5-5 5 5" : "m5 8 5 5 5-5"} />
+          <path d={open ? "M4 4l12 12M16 4 4 16" : "M3 5h14M3 10h14M3 15h14"} />
         </svg>
       </button>
       <nav
diff --git a/apps/pipstart/src/components/reference-page-shell.module.css b/apps/pipstart/src/components/reference-page-shell.module.css
index d5b8a13..c86e1c5 100644
--- a/apps/pipstart/src/components/reference-page-shell.module.css
+++ b/apps/pipstart/src/components/reference-page-shell.module.css
@@ -43,11 +43,16 @@
 @media (max-width: 640px) {
   .header,
   .footer {
-    padding-inline: 1.25rem;
+    justify-content: flex-start;
+    gap: 10px;
+    padding: 12px 18px;
   }
 
   .main {
     width: 100%;
-    padding: 2.5rem 1.25rem 3rem;
+    padding: 20px 18px;
+  }
+  .footer {
+    padding: 12px 18px;
   }
 }
diff --git a/apps/pipstart/src/components/site-chrome.module.css b/apps/pipstart/src/components/site-chrome.module.css
new file mode 100644
index 0000000..34f9102
--- /dev/null
+++ b/apps/pipstart/src/components/site-chrome.module.css
@@ -0,0 +1,28 @@
+.mobileBack {
+  display: none;
+}
+
+@media (max-width: 640px) {
+  .mobileBack {
+    display: flex;
+    align-items: center;
+    color: #0b1220;
+  }
+  .mobileBack svg {
+    width: 18px;
+    height: 18px;
+    fill: none;
+    stroke: currentcolor;
+    stroke-width: 1.8;
+    stroke-linecap: round;
+    stroke-linejoin: round;
+  }
+  .mobileBack + a {
+    display: none;
+  }
+  .section {
+    color: #0b1220 !important;
+    font-size: 13px !important;
+    font-weight: 700 !important;
+  }
+}
diff --git a/apps/pipstart/src/components/site-chrome.tsx b/apps/pipstart/src/components/site-chrome.tsx
index bd9861f..f91d0f3 100644
--- a/apps/pipstart/src/components/site-chrome.tsx
+++ b/apps/pipstart/src/components/site-chrome.tsx
@@ -1,5 +1,6 @@
 import Link from "next/link";
 import type { ReactNode } from "react";
+import styles from "./site-chrome.module.css";
 
 type CompactHeaderProps = {
   className?: string;
@@ -14,10 +15,19 @@ export function CompactHeader({
 }: CompactHeaderProps) {
   return (
     <header className={className}>
+      <Link
+        className={styles.mobileBack}
+        href="/"
+        aria-label="Back to PipStart home"
+      >
+        <svg aria-hidden="true" viewBox="0 0 20 20">
+          <path d="M12 4l-6 6 6 6" />
+        </svg>
+      </Link>
       <Link className={brandClassName} href="/" aria-label="PipStart home">
         PipStart
       </Link>
-      <span>{section}</span>
+      <span className={styles.section}>{section}</span>
     </header>
   );
 }
