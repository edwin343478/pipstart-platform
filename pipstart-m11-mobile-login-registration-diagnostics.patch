diff --git a/apps/pipstart/e2e/milestone-11-accounts.spec.ts b/apps/pipstart/e2e/milestone-11-accounts.spec.ts
index 2363716..33fc914 100644
--- a/apps/pipstart/e2e/milestone-11-accounts.spec.ts
+++ b/apps/pipstart/e2e/milestone-11-accounts.spec.ts
@@ -52,4 +52,18 @@ test.describe("Milestone 11 learner accounts", () => {
     );
     expect(hasOverflow).toBe(false);
   });
+
+  test("shows login in the mobile homepage header and expanded menu", async ({
+    page,
+  }) => {
+    await page.setViewportSize({ width: 390, height: 844 });
+    await page.goto("/");
+    await expect(page.getByRole("link", { name: "Log in" })).toBeVisible();
+    await page.getByRole("button", { name: "Menu" }).click();
+    await expect(
+      page
+        .getByRole("navigation", { name: "Primary navigation" })
+        .getByRole("link", { name: "Log in" }),
+    ).toBeVisible();
+  });
 });
diff --git a/apps/pipstart/src/app/account/actions.ts b/apps/pipstart/src/app/account/actions.ts
index 4cb90a3..8293f39 100644
--- a/apps/pipstart/src/app/account/actions.ts
+++ b/apps/pipstart/src/app/account/actions.ts
@@ -59,6 +59,11 @@ export async function registerAction(
     },
   });
   if (error) {
+    console.error("PipStart registration failed", {
+      code: error.code,
+      message: error.message,
+      status: error.status,
+    });
     return {
       message:
         "We could not create the account. Check your details or try again shortly.",
diff --git a/apps/pipstart/src/app/milestone-11-accounts.test.ts b/apps/pipstart/src/app/milestone-11-accounts.test.ts
index 42f23bc..42b55b4 100644
--- a/apps/pipstart/src/app/milestone-11-accounts.test.ts
+++ b/apps/pipstart/src/app/milestone-11-accounts.test.ts
@@ -38,6 +38,17 @@ describe("Milestone 11 learner accounts", () => {
     expect(actions).not.toContain("No account exists");
   });
 
+  it("logs only privacy-safe registration diagnostics", () => {
+    const actions = read("account/actions.ts");
+    const diagnostic = actions.match(
+      /console\.error\("PipStart registration failed", \{[\s\S]*?\n\s+\}\);/,
+    )?.[0];
+    expect(diagnostic).toContain("code: error.code");
+    expect(diagnostic).toContain("status: error.status");
+    expect(diagnostic).not.toContain("email");
+    expect(diagnostic).not.toContain("password");
+  });
+
   it("protects user pages and the administrator boundary on the server", () => {
     expect(read("account/settings/page.tsx")).toContain("requireUser()");
     expect(read("admin/page.tsx")).toContain("requireAdministrator()");
diff --git a/apps/pipstart/src/app/page.module.css b/apps/pipstart/src/app/page.module.css
index cc3d452..9c6c7c0 100644
--- a/apps/pipstart/src/app/page.module.css
+++ b/apps/pipstart/src/app/page.module.css
@@ -68,6 +68,9 @@
 .signIn:focus-visible {
   background: #115e59;
 }
+.mobileSignIn {
+  display: none;
+}
 .hero {
   padding: 4.75rem 3.5rem 3.75rem;
 }
@@ -251,6 +254,24 @@
     font-weight: 600;
     cursor: pointer;
   }
+  .mobileSignIn {
+    display: inline-flex;
+    min-height: 2.75rem;
+    align-items: center;
+    justify-content: center;
+    margin-left: auto;
+    border-radius: 0.5rem;
+    background: #0f766e;
+    padding: 0.65rem 1rem;
+    color: #fff;
+    font-size: 0.8125rem;
+    font-weight: 700;
+    text-decoration: none;
+  }
+  .mobileSignIn:hover,
+  .mobileSignIn:focus-visible {
+    background: #115e59;
+  }
   .menuButton:hover,
   .menuButton:focus-visible {
     border-color: #0f766e;
@@ -301,9 +322,6 @@
     padding-right: 1.25rem;
     padding-left: 1.25rem;
   }
-  .signIn {
-    display: none;
-  }
   .hero {
     padding-top: 2.5rem;
     padding-bottom: 1.75rem;
diff --git a/apps/pipstart/src/app/page.tsx b/apps/pipstart/src/app/page.tsx
index 38d16b9..3a25a6b 100644
--- a/apps/pipstart/src/app/page.tsx
+++ b/apps/pipstart/src/app/page.tsx
@@ -69,6 +69,7 @@ export default function Home() {
           analysisLinkClassName={styles.analysisLink}
           className={styles.navigation}
           menuButtonClassName={styles.menuButton}
+          mobileSignInClassName={styles.mobileSignIn}
           openClassName={styles.navigationOpen}
           signInClassName={styles.signIn}
         />
diff --git a/apps/pipstart/src/components/primary-navigation.tsx b/apps/pipstart/src/components/primary-navigation.tsx
index fa2f6b3..7294d5f 100644
--- a/apps/pipstart/src/components/primary-navigation.tsx
+++ b/apps/pipstart/src/components/primary-navigation.tsx
@@ -26,6 +26,7 @@ type PrimaryNavigationProps = {
   analysisLinkClassName?: string;
   className?: string;
   menuButtonClassName?: string;
+  mobileSignInClassName?: string;
   openClassName?: string;
   signInClassName?: string;
 };
@@ -34,6 +35,7 @@ export function PrimaryNavigation({
   analysisLinkClassName,
   className,
   menuButtonClassName,
+  mobileSignInClassName,
   openClassName,
   signInClassName,
 }: PrimaryNavigationProps) {
@@ -70,6 +72,9 @@ export function PrimaryNavigation({
     .join(" ");
   return (
     <>
+      <Link className={mobileSignInClassName} href="/login">
+        Log in
+      </Link>
       <button
         ref={menuButtonRef}
         type="button"
