diff --git a/apps/pipstart/e2e/milestone-7-hardening.spec.ts b/apps/pipstart/e2e/milestone-7-hardening.spec.ts
--- a/apps/pipstart/e2e/milestone-7-hardening.spec.ts
+++ b/apps/pipstart/e2e/milestone-7-hardening.spec.ts
@@ -77,7 +77,7 @@ test.describe("Milestone 7 production hardening", () => {
   }) => {
     await page.setViewportSize({ width: 360, height: 800 });
     await page.goto("/learn/forex");
-    const viewMore = page.getByRole("button", { name: /View more levels/i });
+    const viewMore = page.locator('button[aria-controls="forex-levels"]');
     await viewMore.focus();
     await page.keyboard.press("Space");
     await expect(viewMore).toHaveAttribute("aria-expanded", "true");
