diff --git a/apps/pipstart/e2e/milestone-11-accounts.spec.ts b/apps/pipstart/e2e/milestone-11-accounts.spec.ts
index e03f654..2784a5c 100644
--- a/apps/pipstart/e2e/milestone-11-accounts.spec.ts
+++ b/apps/pipstart/e2e/milestone-11-accounts.spec.ts
@@ -76,6 +76,20 @@ test.describe("Milestone 11 learner accounts", () => {
     expect(hasOverflow).toBe(false);
   });
 
+  test("keeps account footers at the mobile viewport edge", async ({
+    page,
+  }) => {
+    await page.setViewportSize({ width: 390, height: 844 });
+    for (const route of ["/register", "/login", "/verify-email"]) {
+      await page.goto(route);
+      const footer = await page.locator("footer").boundingBox();
+      expect(footer).not.toBeNull();
+      expect((footer?.y ?? 0) + (footer?.height ?? 0)).toBeGreaterThanOrEqual(
+        844 - 70,
+      );
+    }
+  });
+
   test("shows login in the mobile homepage header and expanded menu", async ({
     page,
   }) => {
diff --git a/apps/pipstart/src/app/milestone-11-accounts.test.ts b/apps/pipstart/src/app/milestone-11-accounts.test.ts
index 5b979cd..6632dd9 100644
--- a/apps/pipstart/src/app/milestone-11-accounts.test.ts
+++ b/apps/pipstart/src/app/milestone-11-accounts.test.ts
@@ -53,6 +53,25 @@ describe("Milestone 11 learner accounts", () => {
     expect(config).toContain('max_frequency = "60s"');
   });
 
+  it("uses the approved font tokens and a responsive sticky footer on account pages", () => {
+    const shell = read("../components/reference-page-shell.module.css");
+    const accountShell = read("../components/account-shell.module.css");
+    expect(shell).toContain("display: flex");
+    expect(shell).toContain("flex-direction: column");
+    expect(shell).toContain("flex: 1 0 auto");
+    expect(shell).toContain("calc(100dvh - var(--mobile-tab-height))");
+    expect(shell).toContain("var(--font-pipstart-body)");
+    expect(accountShell).toContain("var(--font-pipstart-heading)");
+  });
+
+  it("directs local verification email to Mailpit only in development", () => {
+    const shell = read("../components/account-shell.tsx");
+    const verifyPage = read("verify-email/page.tsx");
+    expect(shell).toContain('process.env.NODE_ENV !== "development"');
+    expect(shell).toContain("http://127.0.0.1:54324");
+    expect(verifyPage).toContain("<LocalEmailNotice />");
+  });
+
   it("logs only privacy-safe registration diagnostics", () => {
     const actions = read("account/actions.ts");
     const diagnostic = actions.match(
diff --git a/apps/pipstart/src/app/verify-email/page.tsx b/apps/pipstart/src/app/verify-email/page.tsx
index fb19f60..bd11de2 100644
--- a/apps/pipstart/src/app/verify-email/page.tsx
+++ b/apps/pipstart/src/app/verify-email/page.tsx
@@ -2,7 +2,7 @@ import type { Metadata } from "next";
 import Link from "next/link";
 
 import { ResendVerificationForm } from "@/components/account-forms";
-import { AccountShell } from "@/components/account-shell";
+import { AccountShell, LocalEmailNotice } from "@/components/account-shell";
 import { getPendingVerificationEmail } from "@/lib/auth/pending-verification";
 
 export const metadata: Metadata = {
@@ -21,6 +21,7 @@ export default async function VerifyEmailPage() {
         For your security, the link expires. Request a fresh confirmation if
         needed.
       </p>
+      <LocalEmailNotice />
       <ResendVerificationForm hasPendingEmail={Boolean(pendingEmail)} />
       <p>
         <Link href="/login">Return to login</Link>
diff --git a/apps/pipstart/src/components/account-shell.module.css b/apps/pipstart/src/components/account-shell.module.css
index 3fefa15..984ad9c 100644
--- a/apps/pipstart/src/components/account-shell.module.css
+++ b/apps/pipstart/src/components/account-shell.module.css
@@ -13,6 +13,8 @@
 
 .heading h1 {
   margin: 0 0 0.75rem;
+  font-family:
+    var(--font-pipstart-heading), var(--font-pipstart-body), Arial, sans-serif;
   font-size: 1.875rem;
   line-height: 1.25;
 }
@@ -49,6 +51,23 @@
   color: var(--brand-accent);
 }
 
+.developmentNotice {
+  margin: 0 0 1rem;
+  border: 1px solid var(--warning-border);
+  border-radius: 0.625rem;
+  background: var(--warning-soft);
+  padding: 0.75rem;
+  color: var(--warning);
+  font-size: 0.78rem;
+  line-height: 1.55;
+}
+
+.developmentNotice a {
+  color: var(--brand-accent);
+  font-weight: 700;
+  text-underline-offset: 0.15rem;
+}
+
 @media (max-width: 767px) {
   .heading {
     margin-bottom: 18px;
diff --git a/apps/pipstart/src/components/account-shell.tsx b/apps/pipstart/src/components/account-shell.tsx
index 4502f7e..9155e62 100644
--- a/apps/pipstart/src/components/account-shell.tsx
+++ b/apps/pipstart/src/components/account-shell.tsx
@@ -12,6 +12,20 @@ type AccountShellProps = {
   title: string;
 };
 
+export function LocalEmailNotice() {
+  if (process.env.NODE_ENV !== "development") return null;
+
+  return (
+    <aside className={styles.developmentNotice} role="status">
+      Local development email is captured by Mailpit. Open the{" "}
+      <a href="http://127.0.0.1:54324" rel="noreferrer" target="_blank">
+        local email inbox
+      </a>{" "}
+      to get your verification link.
+    </aside>
+  );
+}
+
 export function AccountShell({
   children,
   description,
diff --git a/apps/pipstart/src/components/reference-page-shell.module.css b/apps/pipstart/src/components/reference-page-shell.module.css
index a444c49..0b39ef7 100644
--- a/apps/pipstart/src/components/reference-page-shell.module.css
+++ b/apps/pipstart/src/components/reference-page-shell.module.css
@@ -1,7 +1,10 @@
 .page {
+  display: flex;
+  flex-direction: column;
   min-height: 100vh;
   background: #f5f7f8;
   color: #0b1220;
+  font-family: var(--font-pipstart-body), Arial, sans-serif;
 }
 
 .header {
@@ -27,20 +30,29 @@
 }
 
 .main {
+  flex: 1 0 auto;
   width: min(100%, 40rem);
   margin: 0 auto;
   padding: 2.5rem 3.5rem 3rem;
 }
 
 .footer {
+  flex: 0 0 auto;
+  width: 100%;
   padding: 1rem 2.75rem;
   border-top: 1px solid #d9e0e5;
   background: #fff;
   color: #667085;
   font-size: 0.6875rem;
+  line-height: 1.5;
+  overflow-wrap: anywhere;
 }
 
 @media (max-width: 767px) {
+  .page {
+    min-height: calc(100dvh - var(--mobile-tab-height));
+  }
+
   .header,
   .footer {
     justify-content: flex-start;
