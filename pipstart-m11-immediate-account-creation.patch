diff --git a/apps/pipstart/e2e/milestone-11-accounts.spec.ts b/apps/pipstart/e2e/milestone-11-accounts.spec.ts
index cf1f5be..87f43ba 100644
--- a/apps/pipstart/e2e/milestone-11-accounts.spec.ts
+++ b/apps/pipstart/e2e/milestone-11-accounts.spec.ts
@@ -28,42 +28,38 @@ test.describe("Milestone 11 learner accounts", () => {
     ).toBeVisible();
   });
 
-  test("counts down to passwordless verification resend", async ({
-    context,
+  test("welcomes a newly created learner with clear next steps", async ({
     page,
   }) => {
-    await context.addCookies([
-      {
-        httpOnly: true,
-        name: "pipstart_pending_verification",
-        sameSite: "Lax",
-        url: "http://127.0.0.1:3100",
-        value: "learner@example.com",
-      },
-    ]);
-    await page.goto("/verify-email");
-    await expect(page.getByLabel("Email address")).toHaveCount(0);
+    await page.goto("/welcome");
     await expect(
-      page.getByRole("button", { name: "Resend available in 00:60" }),
-    ).toBeDisabled();
+      page.getByRole("heading", { name: "Welcome to PipStart" }),
+    ).toBeVisible();
+    await expect(page.getByText(/You’re all set!/)).toBeVisible();
     await expect(
-      page.getByText("You do not need to enter your address again."),
+      page.getByRole("link", { name: "Start learning" }),
     ).toBeVisible();
   });
 
-  test("explains successful email confirmation and the next step", async ({
+  test("preserves completed registration fields after a missing step", async ({
     page,
   }) => {
-    await page.goto("/email-confirmed");
-    await expect(
-      page.getByRole("heading", { name: "Email confirmed" }),
-    ).toBeVisible();
-    await expect(
-      page.getByText("Your PipStart learner account is ready."),
-    ).toBeVisible();
+    await page.goto("/register");
+    await page.getByLabel("Display name").fill("Asha Learner");
+    await page.getByLabel("Email address").fill("asha@example.com");
+    await page.getByLabel("Password (6+ characters)").fill("learn1");
+    await page.getByLabel("Confirm password").fill("learn1");
+    await page.getByRole("button", { name: "Create account" }).click();
     await expect(
-      page.getByRole("link", { name: "Log in to continue" }),
+      page.getByText("You must accept the Terms and Privacy Policy."),
     ).toBeVisible();
+    await expect(page.getByLabel("Display name")).toHaveValue("Asha Learner");
+    await expect(page.getByLabel("Email address")).toHaveValue(
+      "asha@example.com",
+    );
+    await expect(page.getByLabel("Password (6+ characters)")).toHaveValue(
+      "learn1",
+    );
   });
 
   test("redirects anonymous learners away from account and administrator pages", async ({
@@ -95,7 +91,7 @@ test.describe("Milestone 11 learner accounts", () => {
     page,
   }) => {
     await page.setViewportSize({ width: 390, height: 844 });
-    for (const route of ["/register", "/login", "/verify-email"]) {
+    for (const route of ["/register", "/login", "/welcome"]) {
       await page.goto(route);
       const footer = await page.locator("#main-content + footer").boundingBox();
       expect(footer).not.toBeNull();
diff --git a/apps/pipstart/src/app/account/actions.ts b/apps/pipstart/src/app/account/actions.ts
index 50e56fd..669602f 100644
--- a/apps/pipstart/src/app/account/actions.ts
+++ b/apps/pipstart/src/app/account/actions.ts
@@ -4,11 +4,6 @@ import { revalidatePath } from "next/cache";
 import { redirect } from "next/navigation";
 
 import { requireUser } from "@/lib/auth/session";
-import {
-  getPendingVerificationEmail,
-  rememberPendingVerification,
-  VERIFICATION_RESEND_SECONDS,
-} from "@/lib/auth/pending-verification";
 import {
   safeInternalRedirect,
   validateDisplayName,
@@ -22,7 +17,6 @@ import { createSupabaseServerClient } from "@/lib/supabase/server";
 export type AccountActionState = {
   fieldErrors?: Record<string, string>;
   message?: string;
-  retryAfterSeconds?: number;
   status: "error" | "idle" | "success";
 };
 
@@ -61,7 +55,6 @@ export async function registerAction(
     password: password.value,
     options: {
       data: { display_name: displayName.value },
-      emailRedirectTo: `${getSiteUrl()}/auth/callback?next=/email-confirmed`,
     },
   });
   if (error) {
@@ -76,8 +69,7 @@ export async function registerAction(
       status: "error",
     };
   }
-  await rememberPendingVerification(email.value);
-  redirect("/verify-email");
+  redirect("/welcome");
 }
 
 export async function loginAction(
@@ -122,37 +114,6 @@ export async function forgotPasswordAction(
   };
 }
 
-export async function resendVerificationAction(
-  _previous: AccountActionState,
-  _formData: FormData,
-): Promise<AccountActionState> {
-  void _previous;
-  void _formData;
-  const email = await getPendingVerificationEmail();
-  if (!email) {
-    return {
-      message:
-        "We could not find a pending registration. Please create your account again.",
-      status: "error",
-    };
-  }
-  const supabase = await createSupabaseServerClient();
-  if (!supabase) return unavailable;
-  await supabase.auth.resend({
-    email,
-    options: {
-      emailRedirectTo: `${getSiteUrl()}/auth/callback?next=/email-confirmed`,
-    },
-    type: "signup",
-  });
-  return {
-    message:
-      "If that registration is awaiting verification, a fresh link is on its way.",
-    retryAfterSeconds: VERIFICATION_RESEND_SECONDS,
-    status: "success",
-  };
-}
-
 export async function resetPasswordAction(
   _previous: AccountActionState,
   formData: FormData,
diff --git a/apps/pipstart/src/app/auth/callback/route.ts b/apps/pipstart/src/app/auth/callback/route.ts
index 627b050..6fcaed8 100644
--- a/apps/pipstart/src/app/auth/callback/route.ts
+++ b/apps/pipstart/src/app/auth/callback/route.ts
@@ -2,7 +2,6 @@ import type { EmailOtpType } from "@supabase/supabase-js";
 import { NextResponse, type NextRequest } from "next/server";
 
 import { safeInternalRedirect } from "@/lib/auth/validation";
-import { clearPendingVerification } from "@/lib/auth/pending-verification";
 import { createSupabaseServerClient } from "@/lib/supabase/server";
 
 export async function GET(request: NextRequest) {
@@ -19,10 +18,7 @@ export async function GET(request: NextRequest) {
       : tokenHash && type
         ? await supabase.auth.verifyOtp({ token_hash: tokenHash, type })
         : { error: new Error("Missing authentication token") };
-    if (!result.error) {
-      await clearPendingVerification();
-      return NextResponse.redirect(new URL(next, url.origin));
-    }
+    if (!result.error) return NextResponse.redirect(new URL(next, url.origin));
   }
 
   return NextResponse.redirect(
diff --git a/apps/pipstart/src/app/email-confirmed/page.tsx b/apps/pipstart/src/app/email-confirmed/page.tsx
deleted file mode 100644
index 464a3ab..0000000
--- a/apps/pipstart/src/app/email-confirmed/page.tsx
+++ /dev/null
@@ -1,43 +0,0 @@
-import type { Metadata } from "next";
-import Link from "next/link";
-
-import { AccountShell } from "@/components/account-shell";
-import { getCurrentUser } from "@/lib/auth/session";
-
-import styles from "./page.module.css";
-
-export const metadata: Metadata = {
-  title: "Email confirmed",
-  robots: { follow: false, index: false },
-};
-
-export default async function EmailConfirmedPage() {
-  const user = await getCurrentUser();
-
-  return (
-    <AccountShell
-      description="Your email address has been verified successfully."
-      title="Email confirmed"
-    >
-      <div className={styles.notice} role="status">
-        Your PipStart learner account is ready.
-      </div>
-      <p>
-        {user
-          ? "Continue to your account or return to the learning paths."
-          : "Log in with the email address and password you registered, then continue learning."}
-      </p>
-      <div className={styles.actions}>
-        <Link
-          className={styles.primaryAction}
-          href={user ? "/account/settings" : "/login?notice=email-confirmed"}
-        >
-          {user ? "Continue to my account" : "Log in to continue"}
-        </Link>
-        <Link className={styles.secondaryAction} href="/start-here">
-          Browse learning paths
-        </Link>
-      </div>
-    </AccountShell>
-  );
-}
diff --git a/apps/pipstart/src/app/login/page.tsx b/apps/pipstart/src/app/login/page.tsx
index 75b5e04..8745fe5 100644
--- a/apps/pipstart/src/app/login/page.tsx
+++ b/apps/pipstart/src/app/login/page.tsx
@@ -19,13 +19,11 @@ export default async function LoginPage({
   if (await getCurrentUser()) redirect("/account/settings");
   const { next, notice } = await searchParams;
   const description =
-    notice === "email-confirmed"
-      ? "Your email is confirmed. Log in to continue to your PipStart learner account."
-      : notice === "invalid-link"
-        ? "That authentication link is invalid or has expired. Log in or request a fresh link."
-        : notice === "signed-out"
-          ? "You have been signed out safely."
-          : "Welcome back. Continue with your PipStart learner account.";
+    notice === "invalid-link"
+      ? "That authentication link is invalid or has expired. Log in or request a fresh link."
+      : notice === "signed-out"
+        ? "You have been signed out safely."
+        : "Welcome back. Continue with your PipStart learner account.";
   return (
     <AccountShell description={description} title="Log in">
       <LoginForm next={safeInternalRedirect(next)} />
diff --git a/apps/pipstart/src/app/milestone-11-accounts.test.ts b/apps/pipstart/src/app/milestone-11-accounts.test.ts
index bdb2fae..c894353 100644
--- a/apps/pipstart/src/app/milestone-11-accounts.test.ts
+++ b/apps/pipstart/src/app/milestone-11-accounts.test.ts
@@ -16,8 +16,7 @@ const read = (relativePath: string) =>
 describe("Milestone 11 learner accounts", () => {
   it.each([
     "register/page.tsx",
-    "verify-email/page.tsx",
-    "email-confirmed/page.tsx",
+    "welcome/page.tsx",
     "login/page.tsx",
     "forgot-password/page.tsx",
     "reset-password/page.tsx",
@@ -36,32 +35,36 @@ describe("Milestone 11 learner accounts", () => {
     const actions = read("account/actions.ts");
     expect(actions).toContain("Email or password is incorrect.");
     expect(actions).toContain("If an account matches that email");
-    expect(actions).toContain("If that registration is awaiting verification");
     expect(actions).not.toContain("No account exists");
   });
 
-  it("remembers the pending address server-side and rate-limits verification resends", () => {
+  it("creates accounts immediately without an email-confirmation step", () => {
     const actions = read("account/actions.ts");
     const form = read("../components/account-forms.tsx");
-    const resendForm = form.match(
-      /export function ResendVerificationForm[\s\S]*?export function ResetPasswordForm/,
-    )?.[0];
     const config = fs.readFileSync(supabaseConfigPath, "utf8");
-    expect(actions).toContain("rememberPendingVerification(email.value)");
-    expect(actions).toContain("getPendingVerificationEmail()");
-    expect(form).toContain("Resend available in 00:");
-    expect(resendForm).not.toContain('name="email"');
-    expect(config).toContain('max_frequency = "60s"');
-    expect(actions.match(/next=\/email-confirmed/g)).toHaveLength(2);
+    expect(actions).toContain('redirect("/welcome")');
+    expect(actions).not.toContain("resendVerificationAction");
+    expect(form).not.toContain("ResendVerificationForm");
+    expect(config).toContain("enable_confirmations = false");
   });
 
-  it("shows confirmation success and an explicit next action", () => {
-    const confirmation = read("email-confirmed/page.tsx");
-    expect(confirmation).toContain("Your email address has been verified");
-    expect(confirmation).toContain("Your PipStart learner account is ready.");
-    expect(confirmation).toContain("Continue to my account");
-    expect(confirmation).toContain("Log in to continue");
-    expect(confirmation).toContain("index: false");
+  it("welcomes new learners with friendly next actions", () => {
+    const welcome = read("welcome/page.tsx");
+    expect(welcome).toContain("You’re all set!");
+    expect(welcome).toContain("Start learning");
+    expect(welcome).toContain("View my account");
+    expect(welcome).toContain("index: false");
+  });
+
+  it("keeps registration values in client state after validation errors", () => {
+    const form = read("../components/account-forms.tsx");
+    expect(form).toContain(
+      'const [displayName, setDisplayName] = useState("")',
+    );
+    expect(form).toContain('const [email, setEmail] = useState("")');
+    expect(form).toContain('const [password, setPassword] = useState("")');
+    expect(form).toContain("value={passwordConfirmation}");
+    expect(form).toContain("checked={termsAccepted}");
   });
 
   it("uses the approved font tokens and a responsive sticky footer on account pages", () => {
@@ -75,14 +78,6 @@ describe("Milestone 11 learner accounts", () => {
     expect(accountShell).toContain("var(--font-pipstart-heading)");
   });
 
-  it("directs local verification email to Mailpit only in development", () => {
-    const shell = read("../components/account-shell.tsx");
-    const verifyPage = read("verify-email/page.tsx");
-    expect(shell).toContain('process.env.NODE_ENV !== "development"');
-    expect(shell).toContain("http://127.0.0.1:54324");
-    expect(verifyPage).toContain("<LocalEmailNotice />");
-  });
-
   it("logs only privacy-safe registration diagnostics", () => {
     const actions = read("account/actions.ts");
     const diagnostic = actions.match(
@@ -107,8 +102,7 @@ describe("Milestone 11 learner accounts", () => {
       "login/page.tsx",
       "forgot-password/page.tsx",
       "reset-password/page.tsx",
-      "verify-email/page.tsx",
-      "email-confirmed/page.tsx",
+      "welcome/page.tsx",
       "account-deleted/page.tsx",
     ]) {
       expect(read(route)).toContain("index: false");
diff --git a/apps/pipstart/src/app/verify-email/page.tsx b/apps/pipstart/src/app/verify-email/page.tsx
deleted file mode 100644
index bd11de2..0000000
--- a/apps/pipstart/src/app/verify-email/page.tsx
+++ /dev/null
@@ -1,31 +0,0 @@
-import type { Metadata } from "next";
-import Link from "next/link";
-
-import { ResendVerificationForm } from "@/components/account-forms";
-import { AccountShell, LocalEmailNotice } from "@/components/account-shell";
-import { getPendingVerificationEmail } from "@/lib/auth/pending-verification";
-
-export const metadata: Metadata = {
-  title: "Verify your email",
-  robots: { follow: false, index: false },
-};
-
-export default async function VerifyEmailPage() {
-  const pendingEmail = await getPendingVerificationEmail();
-  return (
-    <AccountShell
-      description="We sent a verification link to the address you registered. Open it to activate your account."
-      title="Check your email"
-    >
-      <p>
-        For your security, the link expires. Request a fresh confirmation if
-        needed.
-      </p>
-      <LocalEmailNotice />
-      <ResendVerificationForm hasPendingEmail={Boolean(pendingEmail)} />
-      <p>
-        <Link href="/login">Return to login</Link>
-      </p>
-    </AccountShell>
-  );
-}
diff --git a/apps/pipstart/src/app/email-confirmed/page.module.css b/apps/pipstart/src/app/welcome/page.module.css
similarity index 100%
rename from apps/pipstart/src/app/email-confirmed/page.module.css
rename to apps/pipstart/src/app/welcome/page.module.css
diff --git a/apps/pipstart/src/app/welcome/page.tsx b/apps/pipstart/src/app/welcome/page.tsx
new file mode 100644
index 0000000..67f761b
--- /dev/null
+++ b/apps/pipstart/src/app/welcome/page.tsx
@@ -0,0 +1,37 @@
+import type { Metadata } from "next";
+import Link from "next/link";
+
+import { AccountShell } from "@/components/account-shell";
+
+import styles from "./page.module.css";
+
+export const metadata: Metadata = {
+  title: "Welcome to PipStart",
+  robots: { follow: false, index: false },
+};
+
+export default function WelcomePage() {
+  return (
+    <AccountShell
+      description="Your learner account is ready. You can start wherever you feel most curious."
+      title="Welcome to PipStart"
+    >
+      <div className={styles.notice} role="status">
+        You’re all set! Take learning one clear step at a time—we’ll be here to
+        help you along the way.
+      </div>
+      <p>
+        Explore the learning paths now, or visit your account whenever you want
+        to manage your profile and preferences.
+      </p>
+      <div className={styles.actions}>
+        <Link className={styles.primaryAction} href="/start-here">
+          Start learning
+        </Link>
+        <Link className={styles.secondaryAction} href="/account/settings">
+          View my account
+        </Link>
+      </div>
+    </AccountShell>
+  );
+}
diff --git a/apps/pipstart/src/components/account-forms.tsx b/apps/pipstart/src/components/account-forms.tsx
index 92dc545..44c2588 100644
--- a/apps/pipstart/src/components/account-forms.tsx
+++ b/apps/pipstart/src/components/account-forms.tsx
@@ -1,7 +1,7 @@
 "use client";
 
 import Link from "next/link";
-import { useActionState, useEffect, useState } from "react";
+import { useActionState, useState, type ChangeEventHandler } from "react";
 import { useFormStatus } from "react-dom";
 
 import {
@@ -10,7 +10,6 @@ import {
   forgotPasswordAction,
   loginAction,
   registerAction,
-  resendVerificationAction,
   resetPasswordAction,
   updateEmailPreferencesAction,
   updateProfileAction,
@@ -21,7 +20,6 @@ import { PASSWORD_MIN_LENGTH } from "@/lib/auth/validation";
 import styles from "./account-forms.module.css";
 
 const initialAccountActionState: AccountActionState = { status: "idle" };
-const VERIFICATION_RESEND_SECONDS = 60;
 
 function Status({ state }: { state: AccountActionState }) {
   if (!state.message && !state.fieldErrors) return null;
@@ -50,12 +48,16 @@ function PasswordField({
   autocomplete = "new-password",
   label,
   name,
+  onChange,
   state,
+  value,
 }: {
   autocomplete?: "current-password" | "new-password";
   label: string;
   name: string;
+  onChange?: ChangeEventHandler<HTMLInputElement>;
   state: AccountActionState;
+  value?: string;
 }) {
   const [visible, setVisible] = useState(false);
   return (
@@ -66,8 +68,10 @@ function PasswordField({
           aria-invalid={Boolean(state.fieldErrors?.[name])}
           autoComplete={autocomplete}
           name={name}
+          onChange={onChange}
           required
           type={visible ? "text" : "password"}
+          value={value}
         />
         <button type="button" onClick={() => setVisible((current) => !current)}>
           {visible ? "Hide" : "Show"}
@@ -87,24 +91,6 @@ function SubmitButton({ children }: { children: string }) {
   );
 }
 
-function ResendSubmitButton({ seconds }: { seconds: number }) {
-  const { pending } = useFormStatus();
-  const waiting = seconds > 0;
-  return (
-    <button
-      className={styles.submit}
-      disabled={pending || waiting}
-      type="submit"
-    >
-      {pending
-        ? "Sending…"
-        : waiting
-          ? `Resend available in 00:${String(seconds).padStart(2, "0")}`
-          : "Resend verification"}
-    </button>
-  );
-}
-
 function DeleteSubmitButton() {
   const { pending } = useFormStatus();
   return (
@@ -119,6 +105,11 @@ export function RegisterForm() {
     registerAction,
     initialAccountActionState,
   );
+  const [displayName, setDisplayName] = useState("");
+  const [email, setEmail] = useState("");
+  const [password, setPassword] = useState("");
+  const [passwordConfirmation, setPasswordConfirmation] = useState("");
+  const [termsAccepted, setTermsAccepted] = useState(false);
   return (
     <form action={action} className={styles.form} noValidate>
       <Status state={state} />
@@ -128,7 +119,9 @@ export function RegisterForm() {
           aria-invalid={Boolean(state.fieldErrors?.displayName)}
           autoComplete="name"
           name="displayName"
+          onChange={(event) => setDisplayName(event.target.value)}
           required
+          value={displayName}
         />
         <FieldError name="displayName" state={state} />
       </label>
@@ -139,15 +132,19 @@ export function RegisterForm() {
           autoComplete="email"
           inputMode="email"
           name="email"
+          onChange={(event) => setEmail(event.target.value)}
           required
           type="email"
+          value={email}
         />
         <FieldError name="email" state={state} />
       </label>
       <PasswordField
         label={`Password (${PASSWORD_MIN_LENGTH}+ characters)`}
         name="password"
+        onChange={(event) => setPassword(event.target.value)}
         state={state}
+        value={password}
       />
       <p className={styles.hint}>
         Letters, numbers and symbols are all accepted. Use any combination.
@@ -155,10 +152,18 @@ export function RegisterForm() {
       <PasswordField
         label="Confirm password"
         name="passwordConfirmation"
+        onChange={(event) => setPasswordConfirmation(event.target.value)}
         state={state}
+        value={passwordConfirmation}
       />
       <label className={styles.checkbox}>
-        <input name="terms" type="checkbox" value="accepted" />
+        <input
+          checked={termsAccepted}
+          name="terms"
+          onChange={(event) => setTermsAccepted(event.target.checked)}
+          type="checkbox"
+          value="accepted"
+        />
         <span>
           I accept the <Link href="/legal/terms">Terms</Link> and{" "}
           <Link href="/legal/privacy-policy">Privacy Policy</Link>.
@@ -236,55 +241,6 @@ export function ForgotPasswordForm() {
   );
 }
 
-export function ResendVerificationForm({
-  hasPendingEmail,
-}: {
-  hasPendingEmail: boolean;
-}) {
-  const [seconds, setSeconds] = useState(
-    hasPendingEmail ? VERIFICATION_RESEND_SECONDS : 0,
-  );
-  const [state, action] = useActionState(
-    async (previous: AccountActionState, formData: FormData) => {
-      const result = await resendVerificationAction(previous, formData);
-      if (result.status === "success" && result.retryAfterSeconds) {
-        setSeconds(result.retryAfterSeconds);
-      }
-      return result;
-    },
-    initialAccountActionState,
-  );
-
-  useEffect(() => {
-    if (seconds <= 0) return;
-    const timer = window.setTimeout(
-      () => setSeconds((current) => Math.max(0, current - 1)),
-      1000,
-    );
-    return () => window.clearTimeout(timer);
-  }, [seconds]);
-
-  return (
-    <form action={action} className={styles.form} noValidate>
-      <Status state={state} />
-      {hasPendingEmail ? (
-        <>
-          <p className={styles.hint}>
-            If the first email does not arrive, you can request another after
-            one minute. You do not need to enter your address again.
-          </p>
-          <ResendSubmitButton seconds={seconds} />
-        </>
-      ) : (
-        <p className={styles.hint}>
-          This browser does not have a pending registration. Please create your
-          account again to request a verification link.
-        </p>
-      )}
-    </form>
-  );
-}
-
 export function ResetPasswordForm() {
   const [state, action] = useActionState(
     resetPasswordAction,
diff --git a/apps/pipstart/src/components/account-shell.module.css b/apps/pipstart/src/components/account-shell.module.css
index 984ad9c..7076c8d 100644
--- a/apps/pipstart/src/components/account-shell.module.css
+++ b/apps/pipstart/src/components/account-shell.module.css
@@ -51,23 +51,6 @@
   color: var(--brand-accent);
 }
 
-.developmentNotice {
-  margin: 0 0 1rem;
-  border: 1px solid var(--warning-border);
-  border-radius: 0.625rem;
-  background: var(--warning-soft);
-  padding: 0.75rem;
-  color: var(--warning);
-  font-size: 0.78rem;
-  line-height: 1.55;
-}
-
-.developmentNotice a {
-  color: var(--brand-accent);
-  font-weight: 700;
-  text-underline-offset: 0.15rem;
-}
-
 @media (max-width: 767px) {
   .heading {
     margin-bottom: 18px;
diff --git a/apps/pipstart/src/components/account-shell.tsx b/apps/pipstart/src/components/account-shell.tsx
index 9155e62..4502f7e 100644
--- a/apps/pipstart/src/components/account-shell.tsx
+++ b/apps/pipstart/src/components/account-shell.tsx
@@ -12,20 +12,6 @@ type AccountShellProps = {
   title: string;
 };
 
-export function LocalEmailNotice() {
-  if (process.env.NODE_ENV !== "development") return null;
-
-  return (
-    <aside className={styles.developmentNotice} role="status">
-      Local development email is captured by Mailpit. Open the{" "}
-      <a href="http://127.0.0.1:54324" rel="noreferrer" target="_blank">
-        local email inbox
-      </a>{" "}
-      to get your verification link.
-    </aside>
-  );
-}
-
 export function AccountShell({
   children,
   description,
diff --git a/apps/pipstart/src/lib/auth/pending-verification.ts b/apps/pipstart/src/lib/auth/pending-verification.ts
deleted file mode 100644
index 45c34fd..0000000
--- a/apps/pipstart/src/lib/auth/pending-verification.ts
+++ /dev/null
@@ -1,37 +0,0 @@
-import "server-only";
-
-import { cookies } from "next/headers";
-
-import { validateEmail } from "./validation";
-
-export const PENDING_VERIFICATION_COOKIE = "pipstart_pending_verification";
-export const VERIFICATION_RESEND_SECONDS = 60;
-
-const cookieOptions = {
-  httpOnly: true,
-  maxAge: 15 * 60,
-  path: "/",
-  sameSite: "lax" as const,
-  secure: process.env.NODE_ENV === "production",
-};
-
-export async function rememberPendingVerification(email: string) {
-  const cookieStore = await cookies();
-  cookieStore.set(PENDING_VERIFICATION_COOKIE, email, cookieOptions);
-}
-
-export async function getPendingVerificationEmail() {
-  const cookieStore = await cookies();
-  const email = validateEmail(
-    cookieStore.get(PENDING_VERIFICATION_COOKIE)?.value ?? null,
-  );
-  return email.ok ? email.value : null;
-}
-
-export async function clearPendingVerification() {
-  const cookieStore = await cookies();
-  cookieStore.set(PENDING_VERIFICATION_COOKIE, "", {
-    ...cookieOptions,
-    maxAge: 0,
-  });
-}
diff --git a/supabase/config.toml b/supabase/config.toml
index d78360f..3acb6cd 100644
--- a/supabase/config.toml
+++ b/supabase/config.toml
@@ -223,7 +223,7 @@ enable_signup = true
 # addresses. If disabled, only the new email is required to confirm.
 double_confirm_changes = true
 # If enabled, users need to confirm their email address before signing in.
-enable_confirmations = true
+enable_confirmations = false
 # If enabled, users will need to reauthenticate or have logged in recently to change their password.
 secure_password_change = true
 # Controls the minimum amount of time that must pass before sending another signup confirmation or password reset email.
