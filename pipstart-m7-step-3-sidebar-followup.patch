diff --git a/apps/pipstart/src/app/learn/crypto/level-1/page.module.css b/apps/pipstart/src/app/learn/crypto/level-1/page.module.css
index 73a83aa..9c1f3ac 100644
--- a/apps/pipstart/src/app/learn/crypto/level-1/page.module.css
+++ b/apps/pipstart/src/app/learn/crypto/level-1/page.module.css
@@ -220,12 +220,10 @@
   color: #0f766e;
 }
 
+.mobileSidebar,
 .sidebarSummary {
   display: none;
 }
-.sidebarDetails .sidebar {
-  display: block;
-}
 
 @media (max-width: 767px) {
   .header {
@@ -243,6 +241,14 @@
     grid-template-columns: 1fr;
   }
 
+  .desktopSidebar {
+    display: none;
+  }
+
+  .mobileSidebar {
+    display: block;
+  }
+
   .sidebarSummary {
     display: flex;
     align-items: center;
@@ -264,10 +270,10 @@
     stroke: #667085;
     stroke-width: 2;
   }
-  .sidebarDetails[open] .sidebarSummary svg {
+  .mobileSidebar[open] .sidebarSummary svg {
     transform: rotate(180deg);
   }
-  .sidebarDetails:not([open]) .sidebar {
+  .mobileSidebar:not([open]) .sidebar {
     display: none;
   }
   .sidebar {
diff --git a/apps/pipstart/src/app/learn/crypto/level-1/page.tsx b/apps/pipstart/src/app/learn/crypto/level-1/page.tsx
index 1cfcd02..c23157a 100644
--- a/apps/pipstart/src/app/learn/crypto/level-1/page.tsx
+++ b/apps/pipstart/src/app/learn/crypto/level-1/page.tsx
@@ -15,6 +15,35 @@ const lessons = [
 ] as const;
 
 export default function CryptoLevelOnePage() {
+  function renderLessonSidebar(className: string) {
+    return (
+      <aside
+        className={`${styles.sidebar} ${className}`}
+        aria-label="Bitcoin lessons"
+      >
+        <h2>Bitcoin</h2>
+        <nav>
+          {lessons.map((lesson) => {
+            const className =
+              lesson.status === "current"
+                ? styles.currentLesson
+                : styles.upcomingLesson;
+            return (
+              <span
+                aria-current={lesson.status === "current" ? "page" : undefined}
+                className={className}
+                key={lesson.label}
+              >
+                {lesson.label}
+                {lesson.label === "Level 1 quiz" ? " · Coming soon" : ""}
+              </span>
+            );
+          })}
+        </nav>
+      </aside>
+    );
+  }
+
   return (
     <main className={styles.page}>
       <LearningHeader
@@ -28,36 +57,15 @@ export default function CryptoLevelOnePage() {
       />
 
       <div className={styles.lessonLayout}>
-        <details className={styles.sidebarDetails}>
+        {renderLessonSidebar(styles.desktopSidebar)}
+        <details className={styles.mobileSidebar}>
           <summary className={styles.sidebarSummary}>
             <span>Bitcoin</span>
             <svg aria-hidden="true" viewBox="0 0 20 20">
               <path d="m5 8 5 5 5-5" />
             </svg>
           </summary>
-          <aside className={styles.sidebar} aria-label="Bitcoin lessons">
-            <h2>Bitcoin</h2>
-            <nav>
-              {lessons.map((lesson) => {
-                const className =
-                  lesson.status === "current"
-                    ? styles.currentLesson
-                    : styles.upcomingLesson;
-
-                return (
-                  <span
-                    aria-current={
-                      lesson.status === "current" ? "page" : undefined
-                    }
-                    className={className}
-                    key={lesson.label}
-                  >
-                    {lesson.label}
-                  </span>
-                );
-              })}
-            </nav>
-          </aside>
+          {renderLessonSidebar(styles.mobileSidebarContent)}
         </details>
 
         <article className={styles.lesson}>
diff --git a/apps/pipstart/src/app/learn/forex/level-1/forex-lesson.tsx b/apps/pipstart/src/app/learn/forex/level-1/forex-lesson.tsx
index c9fe8d5..9602366 100644
--- a/apps/pipstart/src/app/learn/forex/level-1/forex-lesson.tsx
+++ b/apps/pipstart/src/app/learn/forex/level-1/forex-lesson.tsx
@@ -78,6 +78,46 @@ export default function ForexLessonPage({ lesson }: { lesson: ForexLesson }) {
     }
   }
 
+  function renderLessonSidebar(className: string) {
+    return (
+      <aside
+        className={`${styles.sidebar} ${className}`}
+        aria-label="Forex Kindergarten lessons"
+      >
+        <h2>Forex Kindergarten</h2>
+        <p className={styles.progressSummary} aria-live="polite">
+          {completedLessonSlugs.length} of {forexLessons.length} complete
+        </p>
+        <nav>
+          {forexLessons.map((candidate) => {
+            const current = candidate.slug === lesson.slug;
+
+            return (
+              <Link
+                aria-current={current ? "page" : undefined}
+                className={
+                  current ? styles.currentLesson : styles.upcomingLesson
+                }
+                href={candidate.href}
+                key={candidate.slug}
+              >
+                <span>{candidate.title}</span>
+                {completedLessons.has(candidate.slug) ? (
+                  <span className={styles.completedMarker} aria-hidden="true">
+                    ✓
+                  </span>
+                ) : null}
+              </Link>
+            );
+          })}
+          <span className={styles.upcomingLesson}>
+            Level 1 quiz · Coming soon
+          </span>
+        </nav>
+      </aside>
+    );
+  }
+
   return (
     <main className={styles.page}>
       <LearningHeader
@@ -91,48 +131,15 @@ export default function ForexLessonPage({ lesson }: { lesson: ForexLesson }) {
       />
 
       <div className={styles.lessonLayout}>
-        <details className={styles.sidebarDetails}>
+        {renderLessonSidebar(styles.desktopSidebar)}
+        <details className={styles.mobileSidebar}>
           <summary className={styles.sidebarSummary}>
             <span>Forex Kindergarten</span>
             <svg aria-hidden="true" viewBox="0 0 20 20">
               <path d="m5 8 5 5 5-5" />
             </svg>
           </summary>
-          <aside
-            className={styles.sidebar}
-            aria-label="Forex Kindergarten lessons"
-          >
-            <h2>Forex Kindergarten</h2>
-            <p className={styles.progressSummary} aria-live="polite">
-              {completedLessonSlugs.length} of {forexLessons.length} complete
-            </p>
-            <nav>
-              {forexLessons.map((candidate) => {
-                const current = candidate.slug === lesson.slug;
-
-                return (
-                  <Link
-                    aria-current={current ? "page" : undefined}
-                    className={
-                      current ? styles.currentLesson : styles.upcomingLesson
-                    }
-                    href={candidate.href}
-                    key={candidate.slug}
-                  >
-                    <span>{candidate.title}</span>
-                    {completedLessons.has(candidate.slug) ? (
-                      <span
-                        className={styles.completedMarker}
-                        aria-hidden="true"
-                      >
-                        ✓
-                      </span>
-                    ) : null}
-                  </Link>
-                );
-              })}
-            </nav>
-          </aside>
+          {renderLessonSidebar(styles.mobileSidebarContent)}
         </details>
 
         <article className={styles.lesson}>
diff --git a/apps/pipstart/src/app/learn/forex/level-1/page.module.css b/apps/pipstart/src/app/learn/forex/level-1/page.module.css
index 39a0c41..e4fdcad 100644
--- a/apps/pipstart/src/app/learn/forex/level-1/page.module.css
+++ b/apps/pipstart/src/app/learn/forex/level-1/page.module.css
@@ -81,7 +81,8 @@
   gap: 0.25rem;
 }
 
-.sidebar nav > a {
+.sidebar nav > a,
+.sidebar nav > span {
   display: flex;
   min-height: 2.5rem;
   align-items: center;
@@ -245,12 +246,10 @@
   color: #0f766e;
 }
 
+.mobileSidebar,
 .sidebarSummary {
   display: none;
 }
-.sidebarDetails .sidebar {
-  display: block;
-}
 
 @media (max-width: 767px) {
   .header {
@@ -268,6 +267,14 @@
     grid-template-columns: 1fr;
   }
 
+  .desktopSidebar {
+    display: none;
+  }
+
+  .mobileSidebar {
+    display: block;
+  }
+
   .sidebarSummary {
     display: flex;
     align-items: center;
@@ -289,10 +296,10 @@
     stroke: #667085;
     stroke-width: 2;
   }
-  .sidebarDetails[open] .sidebarSummary svg {
+  .mobileSidebar[open] .sidebarSummary svg {
     transform: rotate(180deg);
   }
-  .sidebarDetails:not([open]) .sidebar {
+  .mobileSidebar:not([open]) .sidebar {
     display: none;
   }
   .sidebar {
diff --git a/apps/pipstart/src/app/milestone-6-acceptance.test.ts b/apps/pipstart/src/app/milestone-6-acceptance.test.ts
index e126930..4224e02 100644
--- a/apps/pipstart/src/app/milestone-6-acceptance.test.ts
+++ b/apps/pipstart/src/app/milestone-6-acceptance.test.ts
@@ -61,4 +61,26 @@ describe("Milestone 6 release acceptance", () => {
       );
     }
   });
+
+  it("keeps permanent desktop and collapsible mobile lesson sidebars", () => {
+    for (const route of [
+      "learn/forex/level-1/forex-lesson.tsx",
+      "learn/crypto/level-1/page.tsx",
+    ]) {
+      const source = read(route);
+      expect(source).toContain("desktopSidebar");
+      expect(source).toContain("mobileSidebar");
+      expect(source).toContain("sidebarSummary");
+      expect(source).toContain("Level 1 quiz");
+    }
+
+    for (const stylesheet of [
+      "learn/forex/level-1/page.module.css",
+      "learn/crypto/level-1/page.module.css",
+    ]) {
+      const styles = read(stylesheet);
+      expect(styles).toContain(".desktopSidebar");
+      expect(styles).toContain(".mobileSidebar");
+    }
+  });
 });
